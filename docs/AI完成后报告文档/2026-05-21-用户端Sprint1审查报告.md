---
日期: 2026-05-21
审查人: 项目优化负责人（核查代理）
范围: 用户端 Sprint 1（commit f31fc4f）+ deploy dry-run 验证
依赖文档:
  - docs/AI完成后报告文档/2026-05-20-用户端优化任务执行清单.md
  - docs/AI完成后报告文档/2026-05-20-用户端Sprint1完成报告.md
---

# 用户端 Sprint 1 审查报告

## 0. 一句话结论

**通过。** 14 项硬改动已逐字落地，3 条闸门全绿，dry-run 上传清单干净不带敏感物。建议在合并 / 发布前完成 3 件人工动作（详见 §6）。

---

## 1. 闸门验证（在审查会话里现场跑过）

| 闸门 | 命令 | 结果 |
|---|---|---|
| 用户后端单测 | `cd backend && npm test -- --runInBand` | ✅ 42 套件 / 251 通过 + 2 skipped / 6.7s |
| 用户前端构建 | `cd ai-xsread-vue3 && npm run build` | ✅ 通过（dist 产物完整） |
| 用户前端单测 | `cd ai-xsread-vue3 && npm test` | ✅ 23 套件 / 107 通过 / 6.9s |

闸门与完成报告 §0 摘要里的数字（251 / 107）完全一致。

---

## 2. 22 项任务逐项核查

### 阶段 1 — 恢复闸门（7/7 编码项 + T-1.8 验证步骤）

| 任务 | 落地点 | 核查方式 | 结论 |
|---|---|---|---|
| T-1.1 fontSize 校验 | [preferenceController.js:57](backend/src/controllers/preferenceController.js#L57) | 读源码 | ✅ `Number(body.fontSize)` |
| T-1.2 novelStatus 测试 | backend/tests/integration/novelStatus.test.js | 测试套件全绿 | ✅ |
| T-1.3 auth 中间件去日志 | [auth.js](backend/src/middlewares/auth.js) | grep 全文无 console.log | ✅ |
| T-1.4 request.js DEV 脱敏 | [request.js:62](ai-xsread-vue3/src/api/request.js#L62) | 读源码 | ✅ 仅 method+url |
| T-1.5 限流改全环境 | [app.js:64-78](backend/src/app.js#L64-L78) | 读源码 | ✅ dev 放宽到 3000/15min |
| T-1.6 /api-docs 门控 | [app.js:91-102](backend/src/app.js#L91-L102) | 读源码 | ✅ 包裹 `if env !== production` |
| T-1.7 CI 删 continue-on-error | .github/workflows/ci.yml | grep 无输出 | ✅ |
| T-1.8 阶段 1 收尾验证 | — | 现场跑闸门 | ✅ |

### 阶段 2 — 个人中心 MVP 收口（2/4，剩下两项是人工动作）

| 任务 | 状态 | 备注 |
|---|---|---|
| T-2.1 user_preferences 迁移 | ✅ | INT UNSIGNED + utf8mb4_unicode_ci + created_at |
| T-2.2 notifications 迁移 | ✅ | INT UNSIGNED + ENUM type + utf8mb4_unicode_ci |
| T-2.3 5 链路浏览器手测 | ⏸ 待你执行 | 需启 DB + 前后端 + 浏览器手点 5 页 |
| T-2.4 个人中心整组 commit | ⏸ 待你执行 | 见 §6 第 2 条 |

### 阶段 3 — 安全加固（5/5）

| 任务 | 落地证据 | 结论 |
|---|---|---|
| T-3.1 NotificationsPage `noopener,noreferrer` | NotificationsPage.vue:46 | ✅ |
| T-3.2 csurf 死代码 + 包卸载 | commit 删 src/middlewares/csrf.js（-107 行）+ package.json 移除依赖（-125 行 lock） | ✅ |
| T-3.3 登录/注册专项限流 10/15min | backend/src/routes/auth.js | ✅ |
| T-3.4 JWT 锁 algorithm: HS256 | [auth.js:20,51](backend/src/middlewares/auth.js#L20) + authController.js | ✅ |
| T-3.5 .gitignore 追加 + 取消跟踪 deploy-cache | .gitignore +17 行 / `git rm --cached` 已执行 | ✅ |

### 阶段 4 — 大文件治理（0/2，**已知推迟**）

| 任务 | 现状 | 红线 | 结论 |
|---|---|---|---|
| T-4.1 ReadingPage.vue | 1030 行 | ≤ 500 | ⏸ 推迟独立 Sprint |
| T-4.2 EnhancedEyeCareMode.vue | 1030 行 | ≤ 500 | ⏸ 推迟独立 Sprint |

**对推迟决策的评估：合理。** 清单 §阶段 4 自己也写了"风险最高，拆大文件容易出回归"，应当与功能改动隔离。但这两项要在下一份清单里安排掉，否则会一直鼓在那。

### 阶段 5 — 收尾（3/3，dry-run 在本次完成）

| 任务 | 状态 |
|---|---|
| T-5.1 4 子工程构建+测试 | ✅ |
| T-5.2 部署 dry-run | ✅（本次审查跑过，见 §3） |
| T-5.3 完成报告 | ✅ [2026-05-20-用户端Sprint1完成报告.md](docs/AI完成后报告文档/2026-05-20-用户端Sprint1完成报告.md) |

---

## 3. 部署 dry-run 审查

执行命令：

```bash
cd backend && python scripts/deploy.py --frontend --backend --dry-run
```

### 前端上传清单（dist/ → /www/wwwroot/xs.momofx.cn/ai-xsread-vue3/dist/）

- 新增/变更：51 个文件
- 保持不变：40 个文件
- 本地已删除：47 个文件（旧 hash 文件，rsync `--delete` 会同步清理）
- rsync 排除规则：`.htaccess`、`.user.ini`、`404.html`、`api/`、`uploads/`、`.well-known/`

**安全性核查**：

| 检查项 | 结果 |
|---|---|
| 含 .env / .env.production | ❌ 不含（dist 里本就没有） |
| 含 verify-*.png | ❌ 不含 |
| 含 .qa-test-output/ | ❌ 不含 |
| 含 stats.html（bundle 分析图，含源码体积统计） | ⚠️ **会上传** |
| 含 source map | ❌ 不含 |

> ⚠️ **stats.html** 是 rollup-plugin-visualizer 产物，包含完整的 bundle 体积分析（源文件名 + 体积 + 依赖关系图）。生产环境暴露它不算严重泄漏，但能让外部窥到代码结构。建议在 vite 配置里把 stats.html 输出到 dist 之外，或加 .htaccess 拦掉，或加到 deploy.py rsync `--exclude` 里。**优先级：低（建议下个 sprint 顺手清掉）**。

### 后端上传清单（backend/ → /www/wwwroot/xs.momofx.cn/backend/）

- 新增/变更：10 个文件
- 保持不变：143 个文件
- 本地已删除：1 个文件（`src/middlewares/csrf.js`，已被 rsync `--delete` 同步）
- rsync 排除规则（关键的几个）：
  - `.env` / `.env.local` / `.env.production`
  - `.git`、`node_modules`、`coverage`、`logs`、`uploads`、`tests`、`tmp`
  - `scripts/.deploy-cache.json`、`scripts/deploy.log`
  - `*.log`、`*.test.js`、`*.spec.js`、`*.sqlite`

**安全性核查：清单内核所有敏感文件都在 exclude 列表里，干净。**

唯一可注意的：`scripts/migrate-covers-to-english.js` 等运维脚本会随后端一起上传到生产服务器（不算敏感，但理论上可以加进 exclude 清单瘦身）。**优先级：极低，不阻断发布**。

### 本次会上传的 10 个后端文件

```
database/migrations/202605180001__add-user-preferences-table.sql   ← T-2.1
database/migrations/202605180002__add-notifications-table.sql      ← T-2.2
database/migrations/202605200001__add_admin_tables.sql             ← 管理端 sprint 2 跑出来的迁移
package-lock.json                                                  ← T-3.2 卸 csurf
package.json                                                       ← T-3.2 卸 csurf
src/app.js                                                         ← T-1.5 / T-1.6
src/controllers/authController.js                                  ← T-3.4 JWT algorithm
src/controllers/preferenceController.js                            ← T-1.1
src/middlewares/auth.js                                            ← T-1.3 + T-3.4
src/routes/auth.js                                                 ← T-3.3 登录限流
```

清单和这次 commit 范围匹配，没有意外漂移。

---

## 4. 工作区卫生

`git status -s` 还有以下未提交内容：

| 文件 | 性质 | 处理建议 |
|---|---|---|
| 11 个 `M`（admin-* 子工程） | 管理端 Sprint 2 产物（commit 66cccee 之后又有继续改动？） | 与本次用户端无关，**不影响发布**，但建议在管理端 Sprint 闭环时一起入仓 |
| `D verify-01-profile-logged-out.png` / `D verify-02-profile-logged-in.png` | 本地手测产物，已被 .gitignore 拦截 | 这两个删除是清白动作，可以下次 commit 顺手提走 |
| 8 个 `??`（docs/AI完成后报告文档/2026-05-20-审查报告-*.md） | 本次输入材料 | 建议入仓（项目历史档案） |
| `?? backend/database/migrations/202605200001__add_admin_tables.sql` | 管理端 sprint 2 迁移 | dry-run 显示会被上传，但本地未 commit；**建议先 commit 再发布**，否则生产能跑、版本控制里没这个迁移 |
| `?? admin-backend/tests/`、`admin-frontend/src/__tests__/` | 管理端测试代码（新增） | 与本次用户端无关 |

⚠️ **`202605200001__add_admin_tables.sql` 这条要重视**——它会被 deploy.py 一起上传到生产，但还没入版本控制。如果今天发布、明天回滚，会找不到这个迁移文件。

### 顺手发现：`ai-xsread-vue3/gaobaozhen/`

`ai-xsread-vue3/` 下有一个名为 `gaobaozhen/` 的目录（包含 index.html / read.html / bookshelf.html / theme.js / README.md），既未在 git 跟踪里，也未在 .gitignore 里——看起来是某次 prototype / 静态原型残留。

- **不影响生产部署**（deploy.py 只 rsync `dist/`，不会传它上去）。
- **影响本地干净度**（git status 里看不到，但实际占空间且容易和真实源码混淆）。
- 建议：要么提交到仓库（如果是有用的设计稿），要么删掉，要么加进 .gitignore。

---

## 5. 主观评价

| 维度 | 评价 |
|---|---|
| 落地完整度 | **优。** 清单里贴的 patch 几乎一字不差，没有偷工减料 |
| commit 质量 | **优。** message 按 AGENTS.md 中文规范，关联了任务编号 |
| 测试自证 | **优。** 三条闸门都跑过，且数字与报告吻合 |
| 留尾合理性 | **可。** 推迟的两项（T-4.1/4.2 大文件拆分）按清单原意推迟；待执行的两项（T-2.3/2.4）属人工动作 |
| 风险控制 | **可。** 没有越界改管理端，符合清单 §7 不在范围提醒 |
| 文档纪律 | **优。** 完成报告对照判据逐项交代，未夸大 |

**唯一扣分项**：`scripts/.deploy-cache.json` 在清单 T-3.5 里要求 `git rm --cached`，commit 显示删除了 230 行，✅ 已执行；但 `gaobaozhen/` 这种游离目录暴露出对工作区的整理还差最后一公里。

---

## 6. 发布前必做（建议按这个顺序）

### 1️⃣ T-2.3 个人中心 5 链路浏览器手测（30 分钟）

```bash
# 启 DB（确保 ai_xsread 库存在）
# 启用户后端
cd backend && npm run dev &
# 启用户前端
cd ../ai-xsread-vue3 && npm run dev &
```

打开 `http://localhost:3008` 登录后逐项验证：

| # | 链路 | 操作 | 预期 |
|---|---|---|---|
| 1 | 编辑资料 | /profile/edit → 改昵称 → 保存 | 接口 200 + 返回 profile 页 |
| 2 | 头像上传 | /profile/edit → 点头像选图 | 上传成功 + 头像更新 |
| 3 | 修改密码 | /security → 改密码（旧 6+ 新 6） | 接口 200 + 让重新登录 |
| 4 | 阅读偏好 | /profile/preferences → 拖字号到 18 → 切夜间主题 | PUT 200，刷新保留 |
| 5 | 通知中心 | /profile/notifications | 见到欢迎种子通知；点条 markRead 200 |

### 2️⃣ T-2.4 + 工作区清理 commit（15 分钟）

```bash
# 把 admin sprint2 迁移 + 用户端 sprint1 残尾入仓
git add backend/database/migrations/202605200001__add_admin_tables.sql \
        docs/AI完成后报告文档/2026-05-20-审查报告-*.md \
        docs/AI完成后报告文档/2026-05-20-用户端优化任务执行清单.md \
        docs/AI完成后报告文档/2026-05-20-管理端优化任务执行清单.md \
        docs/AI完成后报告文档/2026-05-21-用户端Sprint1审查报告.md \
        docs/项目总文档/2026-05-21-后台功能Backlog.md
git rm verify-01-profile-logged-out.png verify-02-profile-logged-in.png
git commit -m "docs: 用户端 Sprint 1 审查报告 + 6 份审查输入材料归档"
```

> ⚠ admin-* 11 个 `M` 文件不要在这里一起 commit——它们属于管理端 Sprint 2 范围，应在管理端清单闭环时单独提交（避免范围混杂）。

### 3️⃣ T-2.4 个人中心 MVP 整组提交核对

按清单 §T-2.4 的文件白名单，确认 commit `4048760 feat(profile): 阶段性提交个人中心MVP进展` 已经完整覆盖了 5 个 profile 页 + 8 个 profile 组件 + 双 store + 后端控制器 + 路由。如果有遗漏，按清单白名单补一次 commit。

### 4️⃣ （可选）下个 sprint 顺手清的 3 件小事

- `gaobaozhen/` 目录决定去留
- vite 配置把 `stats.html` 移出 dist 或在 deploy.py 加 exclude
- 拆 `ReadingPage.vue` 与 `EnhancedEyeCareMode.vue`（T-4.1 / T-4.2）

---

## 7. 发布判定

| 阻塞项 | 状态 |
|---|---|
| 闸门全绿 | ✅ |
| 安全加固落地 | ✅ |
| 敏感文件不会随 deploy 上传 | ✅ |
| 个人中心链路通畅 | ⏸ 需 §6.1 手测 |
| 工作区无遗失迁移 | ⚠️ 需 §6.2 commit 后再发布 |

**结论：完成 §6.1 + §6.2 之后即可候选发布。** 整站对外发布仍需等管理端 Sprint 2 清单的对应判据（默认密码、SQL 注入、Nginx HTTPS 等）一起绿。
