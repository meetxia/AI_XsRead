文档日期：2026-05-23

# 管理端统计与导航修复上线部署报告

## 上线内容

- 涉及 commit：`8a2b41d476cd1c08abb247adfba5b4d4100d12b8`
- 提交说明：`修复: 完善管理端小说编辑与导航发布问题`
- 上线范围：
  - 管理端小说编辑页增加阅读量、收藏量编辑。
  - 管理端批量 TXT 上传新小说时自动生成阅读量、收藏量随机数。
  - 管理端封面上传保存真实 `/uploads/...` 地址，拒绝 `blob:` 临时地址。
  - 管理端封面上传目录改为共享 `backend/uploads/images`。
  - 管理端侧边栏菜单改用绝对路径，修复 `/codes` 继续点击变成 `/codes/codes` 的白屏问题。
- 数据库迁移：无新增迁移；线上累计 28 个迁移，最新版本 `202605220003`。

## 部署前备份

- 数据库备份：`/www/wwwroot/xs.momofx.cn/backups/ai_xsread_before_20260523_225435.sql`
- 部署前 commit 备份：`/www/wwwroot/xs.momofx.cn/backups/before-deploy-commit-20260523_225435.txt`
- 用户后端环境备份：`/www/wwwroot/xs.momofx.cn/backups/backend.env.20260523_225435.backup`
- 管理后端环境备份：`/www/wwwroot/xs.momofx.cn/backups/admin-backend.env.20260523_225435.backup`

## 本地验证结果

- `node -c backend/src/app.js`：通过
- `node -c admin-backend/src/controllers/novelController.js`：通过
- `node -c admin-backend/src/services/uploadService.js`：通过
- `cd backend && npm test`：46 个测试套件通过，268 条通过，2 条跳过
- `cd admin-backend && npm test`：8 个测试套件通过，26 条通过
- `cd admin-frontend && npm test`：4 个测试文件通过，12 条通过
- `cd ai-xsread-vue3 && npm run build`：通过
- `cd admin-frontend && npm run build`：通过
- `git diff --check`：通过

说明：构建与测试中仍存在既有的 `baseline-browser-mapping` / `caniuse-lite` 数据过期提示、Element Plus / ECharts 大 chunk 提示、MySQL2 配置项 warning；本次验证未出现失败。

## 部署执行结果

- 已执行 `python backend/scripts/deploy.py --all-with-admin --dry-run`，命令构造与目标路径确认正常。
- 已执行 `python backend/scripts/deploy.py --all-with-admin`。
- 用户前端：rsync 校验后无新增文件需要上传，首页健康检查 200。
- 用户后端：PM2 `xsread-backend` reload 成功，`/api/health` 返回 200。
- 管理前端：上传 28 个新 hash 文件，删除 27 个旧 hash 文件。
- 管理后端：上传 `.env.example`、`src/controllers/novelController.js`、`src/services/uploadService.js`，PM2 `xsread-admin-backend` reload 成功。

## 部署后验收

- `pm2 status`：`xsread-backend` 与 `xsread-admin-backend` 均为 `online`。
- 用户后端日志：22:55:37 看到“数据库迁移完成”“定时任务已启动”“MOMO小说后端服务启动成功”。
- 管理后端日志：22:56:03 看到数据库连接成功、生产环境 8001 启动。
- `http://127.0.0.1:8005/api/health`：返回 `code: 200`，`db: up`，`jobs.isRunning: true`。
- `http://127.0.0.1:8001/api/health`：返回 `status: ok`。
- `https://xs.momofx.cn/`：HTTP 200。
- `https://admin.xs.momofx.cn/`：HTTP 200。
- `https://admin.xs.momofx.cn/codes`：HTTP 200。

## 已知问题

1. 用户后端 PM2 日志中仍可见部署前历史定时任务噪音，例如 2026-05-23 03:00 数据库备份脚本路径报错、04:00 临时目录不存在；部署后 22:55 的新启动日志正常。
2. OpenSSH/rsync 输出 post-quantum key exchange warning，这是 SSH 算法提示，不影响本次部署。
3. 线上历史数据库中如已存在 `blob:` 封面值，本次代码不会自动修复历史数据，需要后台重新上传封面或执行只读排查后人工处理。

## 回滚预案

如上线后发现严重问题：

1. 停止服务：`pm2 stop xsread-backend xsread-admin-backend`
2. 回滚数据库：使用 `/www/wwwroot/xs.momofx.cn/backups/ai_xsread_before_20260523_225435.sql`
3. 回滚代码：按 `/www/wwwroot/xs.momofx.cn/backups/before-deploy-commit-20260523_225435.txt` 中记录的部署前 commit 恢复
4. 恢复环境文件：使用同批次 `backend.env.20260523_225435.backup` 与 `admin-backend.env.20260523_225435.backup`
5. 重装依赖并重启 PM2：`npm ci --omit=dev` 后 `pm2 start ecosystem.config.js && pm2 save`
