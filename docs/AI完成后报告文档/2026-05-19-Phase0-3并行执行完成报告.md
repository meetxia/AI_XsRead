文档日期：2026-05-19

# Phase 0-3 并行执行完成报告

## 1. 执行背景

本轮工作基于《2026-05-19-下一阶段工作任务书》中 0-3 阶段安排进行，目标是在不扩大产品范围的前提下，先补齐发布闸门、个人中心基础验证、管理端测试与 CI，再对已识别的大文件进行行为保持型拆分治理。

本轮由主负责人统筹，多名子代理并行执行，主负责人负责分工、集成审查、风险收口和最终验证。所有子代理均未提交 Git commit。

## 2. 子代理分工与交付结果

| 子代理 | 阶段 | 负责范围 | 结果 |
|---|---|---|---|
| Worker A | Phase 0 | 管理前端 `Settings` 构建阻断修复 | 修复 QQ 校验正则语法问题，管理前端构建通过 |
| Worker B | Phase 0 | 用户后端发布闸门修复 | 修复偏好字体大小小数误收问题，将小说状态接口改为强认证 |
| Worker C | Phase 2 | 管理后端可测试化 | 拆出 `src/server.js`，新增 Jest/Supertest 基础测试 |
| Worker D | Phase 1 | 用户端个人中心 store 测试 | 新增 preferences/notification store 测试，修复登出态未读数清理 |
| Worker E | Phase 2 | 管理前端测试与 CI | 新增 Vitest、路由守卫测试、登录页渲染测试，并补齐 CI 流程 |
| Worker F | Phase 3 | 用户端评论组件拆分 | `CommentSection.vue` 从 1601 行降至 513 行，拆出 5 个子组件 |
| Worker G | Phase 3 | 用户后端大文件拆分 | `userController.js` 降至 776 行，`novelService.js` 降至 524 行，抽出 helper/service 模块 |
| Worker H | 集成修复 | 用户前端属性测试随机失败 | 修正节流属性测试对 leading + trailing 语义的断言口径 |

主负责人额外完成：同步修复 `src/utils/__tests__/throttleDebounce.pbt.test.js` 中同类潜在随机失败口径，避免全量测试因随机样本再次误判。

## 3. 完成内容

### Phase 0：发布闸门修复

- 修复管理端系统设置页 QQ 校验正则写法，解除前端构建阻断。
- 修复用户阅读偏好 `fontSize` 校验逻辑，避免 `16.5` 被 `parseInt` 截断后误判为合法。
- 将 `/api/novels/:id/status` 从可选认证调整为强认证，避免匿名状态查询语义不清。
- 通过后端测试覆盖校验：偏好控制器测试、小说状态认证路径测试均纳入全量验证。

### Phase 1：个人中心 MVP 基础验证

- 新增用户端 preferences store 测试，覆盖读取、更新、状态同步等核心路径。
- 新增 notification store 测试，覆盖列表、未读数、登出态清理等行为。
- 修复登出态 `fetchList` 只清空列表和总数、不清空未读数的问题。
- 修正节流进度上报属性测试口径，使其符合当前实现的 leading 立即上报 + trailing latest payload 补发语义。

### Phase 2：管理端测试与 CI

- 管理后端拆分应用实例与监听入口：
  - `src/app.js` 只负责创建 Express app 和挂载中间件/路由。
  - `src/server.js` 负责监听端口与优雅关闭。
- 管理后端新增 Jest/Supertest 基础测试，覆盖健康路径、认证保护路径和不存在路径。
- 管理前端新增 Vitest 测试体系：
  - 路由守卫测试：无 token 访问受保护路由会跳转登录，有 token 可进入 dashboard。
  - 登录页基础渲染测试：标题、账号输入框、密码输入框、默认账号提示可渲染。
- CI 新增管理前端测试步骤和管理后端 Jest 测试步骤，管理后端语法检查不再 `continue-on-error`。

### Phase 3：大文件拆分治理

- 用户端评论区：
  - `CommentSection.vue` 从 1601 行降至 513 行。
  - 新增 `comment/` 子组件目录，拆出输入框、评论项、骨架屏、空态、加载更多按钮。
  - 父组件继续保留评论加载、提交、点赞、回复、登录确认、排序筛选和分页等敏感业务状态机。
- 用户后端：
  - `userController.js` 从 859 行降至 776 行。
  - `novelService.js` 从 775 行降至 524 行。
  - 新增 `controllers/user/` helper 模块，承接书架参数、用户资料 patch、统计 DTO 等纯逻辑。
  - 新增 `services/novel/` helper 模块，承接查询构建、分页计算、TXT 文本格式化等纯逻辑。

## 4. 涉及文件列表

### CI 与测试基础设施

- `.github/workflows/ci.yml`
- `admin-backend/package.json`
- `admin-backend/package-lock.json`
- `admin-backend/jest.config.js`
- `admin-backend/src/app.js`
- `admin-backend/src/server.js`
- `admin-backend/tests/app.test.js`
- `admin-frontend/package.json`
- `admin-frontend/package-lock.json`
- `admin-frontend/vitest.config.js`
- `admin-frontend/src/router/index.js`
- `admin-frontend/src/router/index.test.js`
- `admin-frontend/src/views/Login/index.test.js`

### 用户前端

- `ai-xsread-vue3/src/components/novel/CommentSection.vue`
- `ai-xsread-vue3/src/components/novel/comment/CommentComposer.vue`
- `ai-xsread-vue3/src/components/novel/comment/CommentEmptyState.vue`
- `ai-xsread-vue3/src/components/novel/comment/CommentItem.vue`
- `ai-xsread-vue3/src/components/novel/comment/CommentLoadMore.vue`
- `ai-xsread-vue3/src/components/novel/comment/CommentSkeletonList.vue`
- `ai-xsread-vue3/src/composables/__tests__/throttleDebounce.pbt.test.js`
- `ai-xsread-vue3/src/utils/__tests__/throttleDebounce.pbt.test.js`
- `ai-xsread-vue3/src/stores/notification.js`
- `ai-xsread-vue3/src/stores/__tests__/preferences.test.js`
- `ai-xsread-vue3/src/stores/__tests__/notification.test.js`

### 用户后端

- `backend/src/controllers/preferenceController.js`
- `backend/src/controllers/userController.js`
- `backend/src/controllers/user/bookshelfHelpers.js`
- `backend/src/controllers/user/profileHelpers.js`
- `backend/src/controllers/user/statisticsPresenter.js`
- `backend/src/routes/novels.js`
- `backend/src/services/novelService.js`
- `backend/src/services/novel/pagination.js`
- `backend/src/services/novel/queryBuilders.js`
- `backend/src/services/novel/textFormatters.js`

### 管理前端

- `admin-frontend/src/views/Settings/index.vue`

## 5. 验证结果

以下命令均由主负责人在集成后重新执行，结果以最新输出为准。

| 模块 | 命令 | 结果 |
|---|---|---|
| 用户前端 | `cd ai-xsread-vue3 && npm test` | 通过，25 个测试文件、114 个测试通过 |
| 用户前端 | `cd ai-xsread-vue3 && npm run build` | 通过，Vite 构建完成，850 modules transformed |
| 用户后端 | `cd backend && npm test -- --runInBand` | 通过，42 个测试套件全部通过，251 passed，2 skipped |
| 管理前端 | `cd admin-frontend && npm test` | 通过，2 个测试文件、3 个测试通过 |
| 管理前端 | `cd admin-frontend && npm run build` | 通过，Vite 构建完成，2117 modules transformed |
| 管理后端 | `cd admin-backend && npm test` | 通过，1 个测试套件、3 个测试通过 |
| 管理后端 | `cd admin-backend && node -c src/app.js` | 通过，退出码 0 |
| 管理后端 | `cd admin-backend && node -c src/server.js` | 通过，退出码 0 |

## 6. 已知问题与风险

1. 用户后端测试仍有既有环境噪声：
   - MySQL2 对 `acquireTimeout` / `timeout` 配置输出 warning。
   - `novelStatus` 集成测试因本地 DB root 访问失败跳过 2 个 DB happy-path 用例。
   - 全量后端测试命令本次退出码为 0。
2. 前端测试与构建仍有 Browserslist / baseline-browser-mapping 数据过旧提示，不影响本次测试和构建结果。
3. 管理前端构建仍有大 chunk 警告，主要来自 `echarts` 和 `element-plus`，属于后续性能优化事项。
4. 行数扫描显示仍有两个既有超 1000 行文件：
   - `ai-xsread-vue3/src/components/reading/EnhancedEyeCareMode.vue`：1030 行。
   - `ai-xsread-vue3/src/views/ReadingPage.vue`：1030 行。
   本轮已治理的 `CommentSection.vue`、`userController.js`、`novelService.js` 均低于 1000 行。
5. `CommentItem` 保留了图片预览事件 emit，但父组件未接入处理函数。这与拆分前“模板绑定但实际无预览函数”的既有行为等价，本轮未扩展图片预览功能。
6. 当前变更尚未进行 Git 提交，等待负责人确认是否按本轮 Phase 0-3 作为一次集成提交。

## 7. 后续建议

1. 下一轮优先处理两个仍超 1000 行的用户前端阅读相关文件，将阅读页状态机、护眼模式配置和展示组件进一步拆分。
2. 对管理前端进行按路由懒加载和 vendor chunk 拆分，优先降低 `echarts` / `element-plus` chunk 体积。
3. 清理后端 MySQL2 已弃用配置项，避免未来 MySQL2 升级后 warning 变为错误。
4. 为管理后端补充更贴近真实业务的控制器级测试，当前测试仅覆盖基础入口和认证保护。
5. 增加评论区组件级测试，覆盖拆分后的发表评论、回复、点赞和加载更多交互。
6. 准备一次 Phase 0-3 集成提交，提交前再次确认 `git diff` 中没有日志、临时产物、密钥或无关文件。
