# 用户端优化任务执行清单（Sprint 1）

文档日期：2026-05-20
范围：用户前端 `ai-xsread-vue3/` + 用户后端 `backend/` + 关联数据库迁移与安全配置
来源：本批 6 份审查报告 + 负责人总报告
不在范围：管理前端 / 管理后端 / Nginx 配置（这些列入下一份清单）

---

## 0. 使用说明

本清单按"先恢复闸门 → 修硬 bug → 收口个人中心 MVP → 安全加固 → 大文件治理"的顺序排，**强烈建议按这个顺序执行**——前面的任务为后面任务铺路（比如先把测试跑绿了，后面拆大文件才有兜底）。

每个任务都给：
- **目标**：做完之后什么样算 done
- **改哪里**：精确到 `文件:行号`
- **改成什么**：贴可直接复用的 patch 片段
- **怎么验证**：可执行的命令 + 预期输出
- **预估**：单人工时
- **依赖**：必须先做完哪些任务

任务编号格式 `T-阶段.序号`，例：`T-1.2` = 阶段 1 的第 2 个任务。

完成一项就在 `[ ]` 打 `[x]`。

---

## 阶段 1 — 恢复闸门（半天，必做）

> 目标：让用户后端 `npm test` 全绿、用户前端 `npm run build` + `npm test` 全绿、CI 不再放过红色。
> 完成判据：4 命令全部 exit 0，且 `.github/workflows/ci.yml` 不再有 `continue-on-error: true`。

### [ ] T-1.1 — 修 fontSize 校验 bug（5 分钟）

**目标**：`preferenceController.fontSize` 拒绝 `16.5` 这种非整数。

**改哪里**：`backend/src/controllers/preferenceController.js:56-62`

**改成什么**：
```js
// 当前（buggy）：
if (body.fontSize !== undefined) {
  const n = Number.parseInt(body.fontSize, 10);
  if (!Number.isInteger(n) || n < 12 || n > 24) {
    return { ok: false, error: 'fontSize 必须为 12-24 之间的整数' };
  }
  patch.font_size = n;
}

// 改成：
if (body.fontSize !== undefined) {
  const n = Number(body.fontSize);
  if (!Number.isInteger(n) || n < 12 || n > 24) {
    return { ok: false, error: 'fontSize 必须为 12-24 之间的整数' };
  }
  patch.font_size = n;
}
```

只改一个词：`Number.parseInt(body.fontSize, 10)` → `Number(body.fontSize)`。`16.5` 经过 `Number()` 得到 `16.5`，`Number.isInteger(16.5)` 是 `false`，被拒绝；合法的 `16` / `"16"` 仍然 `Number.isInteger` 通过。

**怎么验证**：
```bash
cd backend
npm test -- --testPathPattern=preferenceController 2>&1 | tail -20
```
预期：`fontSize 越界拒绝` 这条 test 由失败变通过。

**依赖**：无。

---

### [ ] T-1.2 — 决策并修复 `/novels/:id/status` 鉴权口径（10 分钟）

**目标**：测试断言与控制器口径一致。

**情况说明**：
- 控制器（`backend/src/controllers/novelController.js:419-446`）显式实现"匿名访问返回全 false 占位 + likes/collections 计数"——这是有意为之。
- 测试（`backend/tests/integration/novelStatus.test.js:102-106`）断言匿名时返回 401。
- 路由（`backend/src/routes/novels.js:396-401`）用 `optionalAuth`。

**决策建议**：保留控制器（允许匿名预览，对 SEO 友好），改测试。

**改哪里**：`backend/tests/integration/novelStatus.test.js:102-106`

**改成什么**：
```js
// 当前：
test('GET /api/novels/:id/status returns 401 when no token is provided', async () => {
  const res = await request(app).get('/api/novels/1/status');
  expect(res.status).toBe(401);
  expect(res.body).toHaveProperty('code', 401);
});

// 改成：允许 200（匿名占位）或 503（DB 不可达）
test('GET /api/novels/:id/status returns placeholder for anonymous user', async () => {
  const res = await request(app).get('/api/novels/1/status');
  // 控制器允许匿名，应返回 200 占位结果；DB 不可达时降级 5xx
  expect([200, 500, 503]).toContain(res.status);
  if (res.status === 200) {
    expect(res.body.data).toHaveProperty('isLiked', false);
    expect(res.body.data).toHaveProperty('inBookshelf', false);
  }
});
```

**怎么验证**：
```bash
cd backend
npm test -- --testPathPattern=novelStatus 2>&1 | tail -20
```

**依赖**：无（与 T-1.1 可并行）。

---

### [ ] T-1.3 — 删除认证中间件 token 日志泄漏（5 分钟）

**目标**：消除 `auth.js` 每请求 console.log token 前 20 字符 + userId/username 的泄漏。

**改哪里**：`backend/src/middlewares/auth.js:13-49`

**改成什么**：直接删除 4 处 console.log。

```js
// 当前（13-18 行）：
console.log('🔐 认证检查:', {
  url: req.url,
  method: req.method,
  hasAuthHeader: !!authHeader,
  authHeaderPreview: authHeader ? authHeader.substring(0, 20) + '...' : 'null'
});
// 删除整段。

// 当前（21 行）：
console.log('❌ 认证失败: 未提供认证令牌或格式错误');
// 删除。

// 当前（30-33 行）：
console.log('✅ Token验证成功:', {
  userId: decoded.id,
  username: decoded.username
});
// 删除整段。

// 当前（45-48 行）：
console.log('❌ Token验证失败:', {
  errorName: error.name,
  errorMessage: error.message
});
// 删除整段。
```

**怎么验证**：
```bash
cd backend
grep -n "console.log" src/middlewares/auth.js
```
预期：无输出。

**依赖**：无。

---

### [ ] T-1.4 — 修复用户前端 request.js DEV 模式 token 泄漏（5 分钟）

**目标**：DEV 模式不再向浏览器 console 打印 token 前 20 字符。

**改哪里**：`ai-xsread-vue3/src/api/request.js:60-74`

**改成什么**：
```js
// 当前：
if (token) {
  config.headers['Authorization'] = `Bearer ${token}`
  if (import.meta.env.DEV) {
    console.log('🔑 添加 Token 到请求头:', {
      url: config.url,
      method: config.method,
      tokenPreview: token.substring(0, 20) + '...',
      authHeader: config.headers['Authorization'].substring(0, 30) + '...'
    })
  }
} else if (import.meta.env.DEV) {
  console.log('⚠️ 未找到 Token:', {
    url: config.url,
    method: config.method
  })
}

// 改成：
if (token) {
  config.headers['Authorization'] = `Bearer ${token}`
  if (import.meta.env.DEV) {
    console.debug('[api] →', config.method?.toUpperCase(), config.url, '(authed)')
  }
} else if (import.meta.env.DEV) {
  console.debug('[api] →', config.method?.toUpperCase(), config.url, '(anon)')
}
```

只输出 method + url，不再触碰 token 内容。

**怎么验证**：
```bash
cd ai-xsread-vue3
grep -n "tokenPreview\|authHeader.substring" src/api/request.js
```
预期：无输出。

**依赖**：无。

---

### [ ] T-1.5 — 用户前端构建闸门补限流（仅生产 → 全环境）（5 分钟）

**目标**：用户后端的全局限流不再仅在 production 生效（开发期裸奔会让恶意脚本压测撞 DB）。

**改哪里**：`backend/src/app.js:64-67`

**改成什么**：
```js
// 当前：
// 应用限流（仅在生产环境）
if (config.server.env === 'production') {
  app.use(limiter);
}

// 改成：始终启用，但 dev 环境放宽到 3000/15min（不影响本地开发）
const isProd = config.server.env === 'production';
const effectiveLimiter = isProd ? limiter : rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 3000,
  standardHeaders: true,
  legacyHeaders: false,
  skip: (req) => {
    if (req.path === '/api/health') return true;
    if (req.path.startsWith('/uploads/')) return true;
    if (!req.path.startsWith('/api/')) return true;
    return false;
  }
});
app.use(effectiveLimiter);
```

**怎么验证**：
```bash
cd backend
node -c src/app.js
NODE_ENV=development npm run dev &
sleep 3
curl -s http://localhost:8005/api/health
kill %1
```
预期：health 200，开发期请求不会被快速 429。

**依赖**：无。

---

### [ ] T-1.6 — `/api-docs` 在 production 加门控（5 分钟）

**目标**：生产环境不再裸暴露 swagger UI。

**改哪里**：`backend/src/app.js:80-89`

**改成什么**：
```js
// 当前：
app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerSpec, {
  customCss: '.swagger-ui .topbar { display: none }',
  customSiteTitle: 'MOMO小说 API 文档'
}));

app.get('/api-docs.json', (req, res) => {
  res.setHeader('Content-Type', 'application/json');
  res.send(swaggerSpec);
});

// 改成：仅 NODE_ENV !== 'production' 暴露
if (config.server.env !== 'production') {
  app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerSpec, {
    customCss: '.swagger-ui .topbar { display: none }',
    customSiteTitle: 'MOMO小说 API 文档'
  }));

  app.get('/api-docs.json', (req, res) => {
    res.setHeader('Content-Type', 'application/json');
    res.send(swaggerSpec);
  });
}
```

如果将来有运营 / 合作方需要看文档，再单独加 `EXPOSE_API_DOCS=true` 环境变量门控。

**怎么验证**：
```bash
cd backend
NODE_ENV=production node -c src/app.js
NODE_ENV=production node -e "const app = require('./src/app'); app.listen(8888, () => { require('http').get('http://localhost:8888/api-docs', (r) => { console.log('docs status:', r.statusCode); process.exit(r.statusCode === 404 ? 0 : 1); }); });"
```
预期：`docs status: 404`。

**依赖**：无。

---

### [ ] T-1.7 — CI 删除 `continue-on-error: true`（10 分钟，包括看测试日志的时间）

**目标**：让 CI 测试失败真的红，而不是绿牌假绿。

**改哪里**：`.github/workflows/ci.yml:27,68,101`

**改成什么**：删除三处 `continue-on-error: true` 行。

```yaml
# user-frontend job:
      - name: Vitest
        run: npx vitest run --reporter=verbose
        # 删除下面这行
        # continue-on-error: true

# user-backend job:
      - name: Jest tests
        run: npm test
        # 删除下面这行
        # continue-on-error: true

# admin-backend job:
      - name: Syntax check
        run: node -c src/app.js
        # 删除下面这行
        # continue-on-error: true
```

**怎么验证**：
```bash
grep -n "continue-on-error" .github/workflows/ci.yml
```
预期：无输出。

**依赖**：T-1.1 / T-1.2（先把用户后端测试改绿，再删兜底；否则 CI 会立即变红）。

---

### [ ] T-1.8 — 阶段 1 收尾验证（10 分钟）

**目标**：阶段 1 全部任务做完后跑一遍闸门确认全绿。

**做什么**：
```bash
# 用户后端
cd backend
npm test -- --runInBand 2>&1 | tail -30

# 用户前端
cd ../ai-xsread-vue3
npm run build 2>&1 | tail -20
npm test 2>&1 | tail -20

# 简单语法检查
cd ../backend
node -c src/app.js
```

**预期**：
- backend `npm test`：全绿（253 通过 + 集成 DB 不可达跳过）
- ai-xsread-vue3 `npm run build`：exit 0
- ai-xsread-vue3 `npm test`：107 通过
- `node -c`：无输出

**做完打勾后才能进阶段 2**。

---

## 阶段 2 — 个人中心 MVP 数据库收口（半天）

> 目标：把未提交的 user_preferences / notifications 迁移修好；与代码完全对齐；最后整批入仓。
> 完成判据：迁移幂等可回放；新机器跑完迁移后启动 backend 不报错；前端 5 个 profile 页全部能跑通。

### [ ] T-2.1 — 修 user_preferences 迁移（10 分钟）

**目标**：主键类型对齐 INT、补 created_at、补 collate。

**改哪里**：`backend/database/migrations/202605180001__add-user-preferences-table.sql`

**改成什么**：
```sql
-- 个人中心 MVP - 用户阅读偏好表
SET NAMES utf8mb4;

CREATE TABLE IF NOT EXISTS user_preferences (
  user_id        INT UNSIGNED PRIMARY KEY,
  font_size      TINYINT NOT NULL DEFAULT 16,
  line_height    DECIMAL(3,1) NOT NULL DEFAULT 1.8,
  theme          VARCHAR(16) NOT NULL DEFAULT 'cream',
  page_mode      VARCHAR(16) NOT NULL DEFAULT 'scroll',
  eye_protection TINYINT(1) NOT NULL DEFAULT 0,
  created_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

三处变化：
1. `user_id BIGINT UNSIGNED` → `INT UNSIGNED`（与 users.id 对齐）
2. 加 `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`
3. 加 `COLLATE=utf8mb4_unicode_ci`

**注意**：因为这个迁移**还没提交、还没在生产执行过**，可以直接改。如果已经在某些开发机执行过，新版迁移仍然是 `CREATE TABLE IF NOT EXISTS`，幂等——但本地数据要先 `DROP TABLE user_preferences` 再重跑迁移。

**怎么验证**：
```bash
cd backend
# 本地有 DB 时
mysql -u <user> -p ai_xsread -e "DROP TABLE IF EXISTS user_preferences;"
node -e "require('./src/app'); setTimeout(() => process.exit(0), 3000);" 2>&1 | grep "数据库迁移完成"
```

**依赖**：无。

---

### [ ] T-2.2 — 修 notifications 迁移（10 分钟）

**目标**：user_id 类型对齐 INT、补 collate、补 type ENUM 约束。

**改哪里**：`backend/database/migrations/202605180002__add-notifications-table.sql`

**改成什么**：
```sql
-- 个人中心 MVP - 通知中心表
SET NAMES utf8mb4;

CREATE TABLE IF NOT EXISTS notifications (
  id           INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  user_id      INT UNSIGNED NOT NULL,
  type         ENUM('system', 'membership', 'achievement') NOT NULL,
  title        VARCHAR(128) NOT NULL,
  content      VARCHAR(500) NOT NULL,
  link         VARCHAR(255) DEFAULT NULL,
  is_read      TINYINT(1) NOT NULL DEFAULT 0,
  created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  read_at      DATETIME DEFAULT NULL,
  KEY idx_user_unread (user_id, is_read, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 种子欢迎通知
INSERT INTO notifications (user_id, type, title, content, is_read)
SELECT u.id, 'system', '欢迎来到 MOMO 小说', '读过的书，遇过的人，都会变成你。', 0
FROM users u
WHERE NOT EXISTS (
  SELECT 1 FROM notifications n
  WHERE n.user_id = u.id
    AND n.type = 'system'
    AND n.title = '欢迎来到 MOMO 小说'
);
```

三处变化：
1. `id BIGINT UNSIGNED` → `INT UNSIGNED`
2. `user_id BIGINT UNSIGNED` → `INT UNSIGNED`
3. `type VARCHAR(32)` → `ENUM('system', 'membership', 'achievement')`
4. 加 `COLLATE=utf8mb4_unicode_ci`

**控制器侧**：`backend/src/controllers/notificationController.js` 当前对 type 不做白名单校验。如果未来要 createNotification 入口给后端用，应该校验 type 在 ENUM 内——但本次因为没有 createNotification 端点，先不做。

**依赖**：无。

---

### [ ] T-2.3 — 联调个人中心 5 条链路（1 小时）

**目标**：未提交的 5 个 profile 页全跑通。

**改哪里**：不改代码，只是验证。如果发现 bug 再补改动。

**做什么**：
```bash
# 1. 启 DB（确保 ai_xsread 库存在）
# 2. 启用户后端
cd backend && npm run dev &
# 3. 启用户前端
cd ../ai-xsread-vue3 && npm run dev &
```

**逐项验证**（用浏览器访问 `http://localhost:3008`）：

| 链路 | 步骤 | 预期 |
|---|---|---|
| 1. 编辑资料 | 登录 → /profile/edit → 改昵称 → 保存 | 接口 200 + 返回 profile 页 |
| 2. 头像上传 | /profile/edit → 点头像选图 | 上传成功 + 头像更新 |
| 3. 修改密码 | /security → 改密码（旧 6 位+新 6 位） | 接口 200 + 让重新登录 |
| 4. 阅读偏好 | /profile/preferences → 拖字号到 18 → 切主题为夜间 | 服务端 PUT 200，刷新后保留 |
| 5. 通知 | /profile/notifications | 至少看到"欢迎来到 MOMO 小说"种子通知；点击单条 → markRead 200 |

**遇到问题**：把对应控制器 / store / API 文件路径记到清单底部"待补任务"里。

**依赖**：T-2.1 / T-2.2（迁移要先对）。

---

### [ ] T-2.4 — 个人中心 MVP 整组提交（15 分钟）

**目标**：把工作区所有未提交的"个人中心 MVP"改动作为一组干净 commit 入库。

**做什么**：
```bash
# 1. 先看清楚 diff
git status
git diff --stat

# 2. 排除非个人中心改动（如有）
# 3. 分组 add（按 commit 风格）
git add ai-xsread-vue3/src/api/notifications.js \
        ai-xsread-vue3/src/api/preferences.js \
        ai-xsread-vue3/src/components/profile/AvatarUploader.vue \
        ai-xsread-vue3/src/components/profile/ChangePasswordDialog.vue \
        ai-xsread-vue3/src/components/profile/InterestTagPicker.vue \
        ai-xsread-vue3/src/components/profile/NotificationItem.vue \
        ai-xsread-vue3/src/components/profile/PasswordStrengthBar.vue \
        ai-xsread-vue3/src/stores/notification.js \
        ai-xsread-vue3/src/stores/preferences.js \
        ai-xsread-vue3/src/views/profile/ \
        ai-xsread-vue3/src/router/index.js \
        ai-xsread-vue3/src/views/ProfilePage.vue \
        ai-xsread-vue3/src/views/SecurityPage.vue \
        ai-xsread-vue3/src/components/profile/AchievementCard.vue \
        backend/src/controllers/userController.js \
        backend/src/controllers/notificationController.js \
        backend/src/controllers/preferenceController.js \
        backend/src/routes/index.js \
        backend/src/routes/user.js \
        backend/src/routes/notifications.js \
        backend/database/migrations/202605180001__add-user-preferences-table.sql \
        backend/database/migrations/202605180002__add-notifications-table.sql \
        backend/tests/controllers/notificationController.test.js \
        backend/tests/controllers/preferenceController.test.js \
        docs/AI完成后报告文档/2026-05-18-Agent-A-个人中心后端API交付报告.md \
        docs/AI完成后报告文档/2026-05-18-Agent-B-个人中心编辑资料交付报告.md \
        docs/AI完成后报告文档/2026-05-18-Agent-C-阅读偏好与通知中心交付报告.md \
        docs/AI完成后报告文档/2026-05-19-Agent-D-个人中心收尾联调交付报告.md

# 4. commit（参考 AGENTS.md 中文 + 多行格式）
git commit -m "feat(profile): 个人中心 MVP 整组入仓

- 后端：新增 preferenceController / notificationController + 路由
- 后端：迁移 user_preferences / notifications（INT UNSIGNED + utf8mb4_unicode_ci）
- 前端：新增 5 个 profile 页（edit/preferences/notifications/help/about）
- 前端：新增 8 个 profile 组件 + notification/preferences 双 store
- 前端：useUserStore / readingSettings 与 preferences 双向联动
- 测试：preferenceController + notificationController 单测覆盖

修复：fontSize 校验改用 Number() 拒绝非整数（16.5 不再误通过）"
```

**注意事项**：
- **不要 `git add .`**，避免拖入 `.qa-test-output/`、`verify-*.png`、`backend/scripts/.deploy-cache.json` 这种产物。
- 提交前 `git diff --cached` 再扫一遍。
- 不要带 `.env` / 密钥 / log 文件。

**怎么验证**：
```bash
git log -1 --stat
```

**依赖**：T-1.1 / T-1.2 / T-2.1 / T-2.2 / T-2.3（保证测试绿、迁移对、链路通）。

---

## 阶段 3 — 用户端安全加固（半天）

> 目标：把用户端的"中等风险"全部清掉，让它单独发布也是干净的。
> 完成判据：依赖审计无 high CVE；CSRF/csurf 死代码删掉；NotificationsPage 外链 noreferrer。

### [ ] T-3.1 — NotificationsPage 外链补 noreferrer（1 分钟）

**目标**：外链 `window.open` 防止 referrer 泄漏。

**改哪里**：`ai-xsread-vue3/src/views/profile/NotificationsPage.vue:46`

**改成什么**：
```js
// 当前：
window.open(item.link, '_blank', 'noopener')
// 改成：
window.open(item.link, '_blank', 'noopener,noreferrer')
```

**依赖**：无。

---

### [ ] T-3.2 — 删除 csurf 死代码（5 分钟）

**目标**：csurf 包已 deprecated 且项目里没用，删掉。

**做什么**：
```bash
cd backend
# 1. 确认 csrf.js 中间件确实没在路由挂载
grep -rn "require.*middlewares/csrf\|require.*csurf\|csurfMiddleware" src/
# 预期：只有 src/middlewares/csrf.js 自己

# 2. 删文件
rm src/middlewares/csrf.js

# 3. 卸依赖
npm uninstall csurf

# 4. 重跑测试
npm test -- --runInBand 2>&1 | tail -10
```

**怎么验证**：
```bash
grep -rn "csurf\|csrf\.js" src/ package.json
```
预期：除了与 CSRF 无关的注释外没有匹配。

**依赖**：T-1.1 / T-1.2（测试要先全绿，删完再跑确认没引发回归）。

---

### [ ] T-3.3 — 用户后端补登录限流（10 分钟）

**目标**：`/api/auth/login` / `/api/auth/register` 单独限流（5/15min），与全局限流分开桶。

**改哪里**：`backend/src/routes/auth.js`

**改成什么**：
```js
const express = require('express');
const router = express.Router();
const rateLimit = require('express-rate-limit');
const authController = require('../controllers/authController');
const { authenticate } = require('../middlewares/auth');
const { loginValidation } = require('../utils/validators');

// 登录 / 注册专项限流：每 IP 每 15 分钟 10 次
const authLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 10,
  standardHeaders: true,
  legacyHeaders: false,
  message: {
    code: 429,
    message: '操作过于频繁，请 15 分钟后再试',
    timestamp: Date.now()
  }
});

router.post('/register', authLimiter, authController.register);
router.post('/login', authLimiter, loginValidation, authController.login);
router.post('/refresh', authController.refresh);
router.post('/refresh-token', authController.refresh);
router.get('/me', authenticate, authController.getCurrentUser);
router.post('/change-password', authenticate, authController.changePassword);
router.post('/logout', authenticate, authController.logout);

module.exports = router;
```

**怎么验证**：
```bash
cd backend
node -c src/routes/auth.js
npm test 2>&1 | tail -10
```

**依赖**：阶段 1。

---

### [ ] T-3.4 — JWT 显式锁定 algorithm（5 分钟）

**目标**：避免 jsonwebtoken `algorithm: none` 攻击面。

**改哪里**：
1. `backend/src/middlewares/auth.js:28,69`（验证侧）
2. `backend/src/controllers/authController.js:66-82, 176-192, 240-249`（签发侧）

**改成什么**：
```js
// 验证侧（auth.js:28 和 :69）：
const decoded = jwt.verify(token, config.jwt.secret, { algorithms: ['HS256'] });

// 签发侧（authController.js）— 在每处 jwt.sign 调用上加 algorithm 选项：
const accessToken = jwt.sign(
  { id, username, email, role: role || 'user' },
  config.jwt.secret,
  { expiresIn: config.jwt.expiresIn, algorithm: 'HS256' }
);

const refreshToken = jwt.sign(
  { id: user.id },
  config.jwt.refreshSecret,
  { expiresIn: config.jwt.refreshExpiresIn, algorithm: 'HS256' }
);
```

`refresh` 函数（authController.js:225）的 `jwt.verify` 同样加 `{ algorithms: ['HS256'] }`。

**怎么验证**：
```bash
cd backend
grep -n "algorithm\|algorithms" src/middlewares/auth.js src/controllers/authController.js
npm test 2>&1 | tail -10
```

**依赖**：阶段 1。

---

### [ ] T-3.5 — 清理用户端 git 卫生（10 分钟）

**目标**：根目录截图 / 测试产物不再污染 git。

**改哪里**：`.gitignore`

**改成什么**：在文件末尾追加
```
# 显式忽略环境变量
.env
.env.local
.env.production
backend/.env
admin-backend/.env

# 测试与验证产物
verify-*.png
covers-loaded-after-fix.png
.qa-test-output/

# 部署脚本日志与缓存
backend/scripts/deploy.log
```

注意 `backend/scripts/.deploy-cache.json` 已经在 .gitignore（line 35）但还在被跟踪。需要：
```bash
git rm --cached backend/scripts/.deploy-cache.json
```

并且根目录 3 张 png 应该删除（如不需要）：
```bash
git status -s | grep '\.png$'  # 看一遍再决定
```

**怎么验证**：
```bash
git status -s
```
预期：`?? .qa-test-output/` 和 `?? covers-loaded-after-fix.png` 不再出现。

**依赖**：阶段 2（避免与个人中心 commit 冲突）。

---

## 阶段 4 — 用户端大文件治理（1.5 天）

> 目标：把用户前端两个破 1000 行红线的文件拆下去。
> 完成判据：`ReadingPage.vue` 与 `EnhancedEyeCareMode.vue` 都 ≤ 500 行；阅读核心链路（翻页 / 进度 / TTS / 设置）测试绿。

> ⚠ 这个阶段**风险最高**——拆大文件容易出回归。所以阶段 1 / 2 必须先把测试基线建好再做。

### [ ] T-4.1 — 拆 ReadingPage.vue（1 天）

**目标**：1030 → ≤ 500，业务逻辑下沉到 composables。

**改哪里**：`ai-xsread-vue3/src/views/ReadingPage.vue`

**做什么**（建议拆分方向）：
1. 抽 `useReadingPageController.js` composable —— 章节加载、进度同步、心跳上报、键盘绑定的 orchestration 都放这里
2. 把 settings 抽到 `<SettingPanel>` 子组件（已存在）
3. 章节抽屉用 `<ChapterDrawer>`（已存在）
4. 段评气泡用 `<ParagraphCommentBubble>`（已存在）
5. 顶层 ReadingPage.vue 只负责布局 + 把 controller 暴露的状态分发给子组件

**怎么验证**：
```bash
cd ai-xsread-vue3
wc -l src/views/ReadingPage.vue
npm run build
npm test
# 手动跑一遍：登录 → 进任意小说阅读 → 翻页 / 切章节 / 改字号 / 切主题 / TTS 播放 / 段评气泡
```
预期：行数 ≤ 500；构建通过；测试通过；手测无回归。

**依赖**：阶段 1（测试基线）+ 阶段 2（个人中心已入仓，避免冲突）。

---

### [ ] T-4.2 — 拆 EnhancedEyeCareMode.vue（半天）

**目标**：1030 → ≤ 500。

**改哪里**：`ai-xsread-vue3/src/components/reading/EnhancedEyeCareMode.vue`

**做什么**：抽 `useEyeCareMode` composable，把滤镜计算 / 时间感知 / 用眼监测下沉。视图组件保留 UI。

**怎么验证**：
```bash
wc -l src/components/reading/EnhancedEyeCareMode.vue
npm run build
# 手动跑：开启护眼模式 → 切色温 → 阅读 30s 看是否有提示
```

**依赖**：T-4.1（避免一次拆两个引发交叉回归）。

---

## 阶段 5 — 收尾验证 + 上下游联动（半天）

### [ ] T-5.1 — 4 子工程全量构建+测试（30 分钟）

```bash
cd backend && npm test -- --runInBand
cd ../ai-xsread-vue3 && npm run build && npm test
cd ../admin-frontend && npm run build  # 提示：可能仍失败（管理端不在本清单范围）
cd ../admin-backend && node -c src/app.js
```

**预期**：前两条全绿；admin-frontend 失败属预期（不在本清单范围）；admin-backend 语法检查通过。

---

### [ ] T-5.2 — 部署 dry-run（10 分钟）

```bash
cd backend
python scripts/deploy.py --frontend --backend --dry-run
```

**预期**：列出会上传的文件清单，不真的传；其中**不应当包含** `.env` / `node_modules` / `verify-*.png` / `.qa-test-output/`。

---

### [ ] T-5.3 — 写完成报告（30 分钟）

按 AGENTS.md §"文档规则"，在 `docs/AI完成后报告文档/` 写一份 `2026-05-XX-用户端Sprint1完成报告.md`，至少包含：

- 完成了哪些任务（对照本清单打勾）
- 涉及的文件列表
- 测试结果（命令 + 输出片段）
- 已知问题（个人中心联调中如发现的小 bug）
- 后续建议（特别是阶段 4 大文件拆分进度，或留作下一 Sprint）

---

## 6. 完成判据（Sprint 1 整体）

全部 `[x]` 之后，应该满足：

- [ ] `cd backend && npm test -- --runInBand` exit 0，0 个失败用例
- [ ] `cd ai-xsread-vue3 && npm run build` exit 0
- [ ] `cd ai-xsread-vue3 && npm test` exit 0
- [ ] `grep "console.log" backend/src/middlewares/auth.js` 无输出
- [ ] `grep "tokenPreview" ai-xsread-vue3/src/api/request.js` 无输出
- [ ] `grep "continue-on-error" .github/workflows/ci.yml` 无输出
- [ ] 个人中心 5 条链路在浏览器手测全通
- [ ] 个人中心改动已提交（一组干净 commit）
- [ ] `git status -s` 干净（除非有正在做的阶段 4 拆分）
- [ ] `node scripts/deploy.py --dry-run` 输出合理

完成所有条目即可对外宣称"用户端 Sprint 1 完成，可以候选发布"——但**整站对外发布**还要等管理端清单跑完（默认密码 / SQL 注入 / 构建失败 / Nginx HTTPS 等）。

---

## 7. 不在本清单范围（提醒，以免误碰）

下面这些来自审查报告但**不属于用户端 Sprint 1**，请不要顺手做了——它们要么属于管理端（下一份清单），要么属于长期治理：

- 管理后端 SQL 注入修复（`admin-backend/src/controllers/novelController.js:19,59`）
- 管理后端 trust proxy / JWT 双密钥 / 默认密码强制改
- 管理前端 Settings ASI bug + Login 默认值
- Nginx HTTPS / admin 子域 / 安全响应头
- Express 4 → 5 主版本统一
- bcryptjs 2 → 3 主版本统一
- multer 1 → 2 主版本统一
- 5 张管理表迁入 migrate 流水线
- 用户后端模板拼接 SQL 重构成完全参数化
- 把 logger 框架（pino / winston）替换 console.\*

如果在阶段 4 拆 ReadingPage 时顺手发现某些跨模块 bug，**记下来，下一份清单做**——不要跨范围扩散修改。

---

## 8. 时间预估汇总

| 阶段 | 任务数 | 预估工时（单人） |
|---|---|---|
| 阶段 1 — 恢复闸门 | 8 | 0.5 天 |
| 阶段 2 — 个人中心 MVP 收口 | 4 | 0.5 天（含联调） |
| 阶段 3 — 用户端安全加固 | 5 | 0.5 天 |
| 阶段 4 — 大文件治理 | 2 | 1.5 天 |
| 阶段 5 — 收尾验证 | 3 | 0.5 天 |
| **合计** | **22** | **3.5 个工作日** |

**单人节奏**：从周一到周四下班可以全部完成。
**双人并行**：阶段 1+2 + 阶段 3 可以一人跑（互相依赖），阶段 4 一人独立跑——大约 2.5 天可全部完成。
