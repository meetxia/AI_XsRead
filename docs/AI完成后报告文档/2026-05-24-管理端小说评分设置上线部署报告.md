文档日期：2026-05-24

# 管理端小说评分设置上线部署报告

## 上线内容

- 涉及 commit：`b6b987fdb2e3f2ef369d18c730d341c257f27643`
- 提交说明：`功能: 支持管理端设置小说评分`
- 上线范围：
  - 管理端小说新增与编辑页增加评分设置，范围限制为 0 到 5，并保留两位小数。
  - 管理后端创建与更新小说时保存 `rating` 字段，非法值回退或钳制到有效范围。
  - 管理端 TXT 批量上传新小说时自动生成随机评分。
  - 随机评分默认范围为 `4.2` 到 `4.9`，可通过 `TXT_UPLOAD_RANDOM_RATING_MIN` / `TXT_UPLOAD_RANDOM_RATING_MAX` 配置。
  - 批量上传结果提示补充评分回显。
- 数据库迁移：无新增迁移；复用既有 `novels.rating` 字段。

## 部署前备份

- 数据库备份：`/www/wwwroot/xs.momofx.cn/backups/ai_xsread_before_20260524_233429.sql`
- 数据库备份大小：`1407121` bytes
- 部署前 commit 备份：`/www/wwwroot/xs.momofx.cn/backups/before-deploy-commit.txt`
- 部署前 commit：`c4c742c1e780dfe52f6002e3775096dd08b24436`
- 用户后端环境备份：`/www/wwwroot/xs.momofx.cn/backups/backend.env.backup`
- 管理后端环境备份：`/www/wwwroot/xs.momofx.cn/backups/admin-backend.env.backup`

## 本地验证结果

- `git diff --check`：通过
- `git diff --cached --check`：通过
- `node -c backend/src/app.js`：通过
- `cd admin-backend && npm test -- --runTestsByPath tests/novel-batch-upload.test.js tests/novel-stats-edit.test.js`：2 个测试套件通过，5 条通过
- `cd admin-frontend && npm test -- src/__tests__/novel-edit-cover-upload.test.js`：1 个测试文件通过，4 条通过
- `cd admin-backend && npm test`：8 个测试套件通过，26 条通过
- `cd admin-frontend && npm test`：4 个测试文件通过，12 条通过
- `cd admin-frontend && npm run build`：通过

说明：管理前端构建中仍存在既有的 `baseline-browser-mapping` / `caniuse-lite` 数据过期提示，以及 Element Plus / ECharts 大 chunk 提示；本次验证未出现失败。

## 部署执行结果

- 已执行 `git push origin main`，`main` 从 `8184f63` 更新到 `b6b987f`。
- 已执行 `python backend/scripts/deploy.py --admin --dry-run`，命令构造与目标路径确认正常。
- 已执行 `python backend/scripts/deploy.py --admin`。
- 管理前端：rsync 增量上传 27 个新 hash 文件，删除 26 个旧 hash 文件。
- 管理后端：rsync 上传 `.env.example`、`src/controllers/novelController.js`，PM2 `xsread-admin-backend` reload 成功。
- 管理后端健康检查：`http://127.0.0.1:8001/api/health` 返回 `{"status":"ok"}`。

## 部署后验收

- `pm2 status`：`xsread-backend` 与 `xsread-admin-backend` 均为 `online`。
- 管理后端日志：2026-05-24 23:34:58 看到数据库连接成功、生产环境 8001 启动。
- `http://127.0.0.1:8001/api/health`：返回 `status: ok`。
- `https://admin.xs.momofx.cn/`：HTTP 200。
- 服务器文件 `/www/wwwroot/xs.momofx.cn/admin-backend/src/controllers/novelController.js` 已确认包含 `normalizeRating`、`TXT_UPLOAD_RANDOM_RATING_MIN` / `TXT_UPLOAD_RANDOM_RATING_MAX`、`rating = COALESCE(?, rating)`。

## 已知问题

1. 管理端前端构建仍有浏览器兼容数据过期提示和大 chunk 提示，属于既有构建提示，不影响本次上线。
2. OpenSSH/rsync 输出 post-quantum key exchange warning，这是 SSH 算法提示，不影响本次部署。
3. 本次只部署管理端，未发布工作区中仍未提交的用户端账号/邮箱展示相关改动。

## 回滚预案

如上线后发现严重问题：

1. 停止服务：`pm2 stop xsread-admin-backend`
2. 回滚数据库：使用 `/www/wwwroot/xs.momofx.cn/backups/ai_xsread_before_20260524_233429.sql`
3. 回滚代码：按 `/www/wwwroot/xs.momofx.cn/backups/before-deploy-commit.txt` 中记录的部署前 commit 恢复
4. 恢复环境文件：使用同批次 `backend.env.backup` 与 `admin-backend.env.backup`
5. 重装依赖并重启 PM2：`npm ci --omit=dev` 后 `pm2 start /www/wwwroot/xs.momofx.cn/ecosystem.config.js --only xsread-admin-backend --update-env && pm2 save`
