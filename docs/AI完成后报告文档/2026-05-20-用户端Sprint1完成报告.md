---
日期: 2026-05-20
作者: 项目优化负责人（主代理 + 前端/CI 子代理 + 后端子代理 并行实施）
模块: 用户端 Sprint 1 优化
依赖清单: docs/AI完成后报告文档/2026-05-20-用户端优化任务执行清单.md
---

# 2026-05-20 用户端 Sprint 1 完成报告

## 0. 摘要

按 `2026-05-20-用户端优化任务执行清单.md` 全 22 项任务执行，本次完成 14 项硬改动 + 3 项闸门验证，全部 3 条闸门绿牌通过。剩余 8 项中：T-2.3 / T-2.4 需人工浏览器手测与 commit 决策；T-4.1 / T-4.2 大文件拆分按清单标注"风险最高"原则推迟为独立 Sprint；T-5.2 / T-5.3 在本报告中收尾。

**完成判据对照**（清单 §6）：

| 判据 | 状态 |
|---|---|
| `cd backend && npm test -- --runInBand` exit 0 | ✅ 42 套件 251 通过 + 2 skip |
| `cd ai-xsread-vue3 && npm run build` exit 0 | ✅ |
| `cd ai-xsread-vue3 && npm test` exit 0 | ✅ 23 套件 107 通过 |
| `grep "console.log" backend/src/middlewares/auth.js` 无输出 | ✅ |
| `grep "tokenPreview" ai-xsread-vue3/src/api/request.js` 无输出 | ✅ |
| `grep "continue-on-error" .github/workflows/ci.yml` 无输出 | ✅ |
| 个人中心 5 条链路浏览器手测 | ⏸ 待用户执行（T-2.3） |
| 个人中心改动整组 commit | ⏸ 待用户执行（T-2.4） |
| `git status -s` 干净 | ⏸ 待 commit 后清空 |
| `node scripts/deploy.py --dry-run` 输出合理 | ⏸ 待用户执行（T-5.2） |

## 1. 已完成任务（14 项）

### 阶段 1 — 恢复闸门（7/8）

#### T-1.1 fontSize 校验改 `Number()` 拒非整数
- 文件：`backend/src/controllers/preferenceController.js`
- 改动：`Number.parseInt(body.fontSize, 10)` → `Number(body.fontSize)`
- 效果：`16.5` 被 `Number.isInteger` 拒绝，合法整数仍通过。

#### T-1.2 novelStatus 测试改允许匿名占位
- 文件：`backend/tests/integration/novelStatus.test.js:102-106`
- 改动：断言由 `expect(401)` 改为 `expect([200, 500, 503]).toContain(res.status)`，与控制器允许匿名占位的口径对齐。

#### T-1.3 auth 中间件删除 token 日志泄漏
- 文件：`backend/src/middlewares/auth.js`
- 改动：删除 4 处 `console.log`（含 token 前缀、userId、username、错误堆栈）。
- 验证：`grep -n "console.log" backend/src/middlewares/auth.js` 无输出。

#### T-1.4 前端 request.js DEV 日志脱敏
- 文件：`ai-xsread-vue3/src/api/request.js`
- 改动：DEV 模式从打印 `tokenPreview` / `authHeader.substring` 改为 `console.debug('[api] →', method, url, '(authed|anon)')`。
- 验证：`grep -n "tokenPreview\|authHeader.substring" ai-xsread-vue3/src/api/request.js` 无输出。

#### T-1.5 限流改全环境（dev 放宽 3000/15min）
- 文件：`backend/src/app.js:64-67`
- 改动：限流不再仅 production 启用；dev 使用 3000 次/15min 兜底，跳过 `/api/health` 与 `/uploads/`。

#### T-1.6 /api-docs 加 production 门控
- 文件：`backend/src/app.js:80-89`
- 改动：`/api-docs` 与 `/api-docs.json` 包裹 `if (config.server.env !== 'production')`。生产环境不再裸暴露 swagger UI。

#### T-1.7 CI 删除 `continue-on-error: true`
- 文件：`.github/workflows/ci.yml`
- 改动：删除 user-frontend / user-backend / admin-backend 三处 `continue-on-error: true`。
- 验证：`grep -n "continue-on-error" .github/workflows/ci.yml` 无输出。

#### T-1.8 阶段 1 收尾验证 ✅ 见 §3 闸门验证章节

### 阶段 2 — 个人中心 MVP 数据库收口（2/4）

#### T-2.1 user_preferences 迁移修主键 + collate + created_at
- 文件：`backend/database/migrations/202605180001__add-user-preferences-table.sql`
- 改动：`user_id BIGINT UNSIGNED` → `INT UNSIGNED`（与 users.id 对齐）；新增 `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`；显式 `COLLATE=utf8mb4_unicode_ci`。

#### T-2.2 notifications 迁移修类型 + ENUM + collate
- 文件：`backend/database/migrations/202605180002__add-notifications-table.sql`
- 改动：`id` / `user_id` 改 `INT UNSIGNED`；`type` 由 `VARCHAR(32)` 改 `ENUM('system','membership','achievement')`；显式 `COLLATE=utf8mb4_unicode_ci`；保留种子欢迎通知 `WHERE NOT EXISTS` 幂等条款。

#### T-2.3 / T-2.4 ⏸ 见 §5 待执行项

### 阶段 3 — 用户端安全加固（5/5）

#### T-3.1 NotificationsPage 外链补 `noreferrer`
- 文件：`ai-xsread-vue3/src/views/profile/NotificationsPage.vue:46`
- 改动：`window.open(item.link, '_blank', 'noopener')` → `'noopener,noreferrer'`。

#### T-3.2 csurf 死代码删除 + 卸载
- 删除：`backend/src/middlewares/csrf.js`
- 卸载：`npm uninstall csurf` —— 移除 12 个传递依赖（含 csurf 已 deprecated 包），`package.json` + `package-lock.json` 同步。
- 提示：`backend/scripts/.deploy-cache.json` 中残留的 `src/middlewares/csrf.js` 哈希条目会在下次部署脚本运行时自然刷新，无需手工处理。

#### T-3.3 登录 / 注册补专项限流（10/15min）
- 文件：`backend/src/routes/auth.js`
- 改动：新增 `authLimiter`（windowMs 15min, max 10, standardHeaders true）并应用于 `POST /register` 与 `POST /login`。其他路由继续走全局限流。

#### T-3.4 JWT 显式锁 `algorithm: 'HS256'`
- 文件：`backend/src/middlewares/auth.js`、`backend/src/controllers/authController.js`
- 改动：
  - 验证侧：所有 `jwt.verify(token, secret)` 加 `{ algorithms: ['HS256'] }`（中间件 2 处 + authController.refresh 1 处）
  - 签发侧：所有 `jwt.sign(...)` 在选项对象加 `algorithm: 'HS256'`（authController 4 处：register、login、refresh 的 access、refresh 的 refresh）
- 效果：杜绝 `algorithm: none` 攻击面。

#### T-3.5 git 卫生清理
- 文件：`.gitignore`
- 改动：末尾追加 11 行 —— 显式忽略 `.env`、`backend/.env`、`admin-backend/.env`、`verify-*.png`、`covers-loaded-after-fix.png`、`.qa-test-output/`、`backend/scripts/deploy.log`。
- 取消跟踪：`git rm --cached backend/scripts/.deploy-cache.json`（仅清 index，保留本地文件）。
- 待用户决策的 png：根目录 `verify-01-profile-logged-out.png` / `verify-02-profile-logged-in.png` 此前已被本地删除，git 状态显示 ` D`，可在下次 commit 一并落账。

### 阶段 5 — 收尾验证（部分）

#### T-5.1 用户端 4 条闸门 ✅ 见 §3
#### T-5.2 部署 dry-run ⏸ 见 §5
#### T-5.3 完成报告 ✅ 即本文件

## 2. 涉及文件清单

### 后端
- `backend/src/app.js`
- `backend/src/controllers/authController.js`
- `backend/src/controllers/preferenceController.js`
- `backend/src/middlewares/auth.js`
- `backend/src/middlewares/csrf.js` —— 删除
- `backend/src/routes/auth.js`
- `backend/database/migrations/202605180001__add-user-preferences-table.sql`
- `backend/database/migrations/202605180002__add-notifications-table.sql`
- `backend/tests/integration/novelStatus.test.js`
- `backend/package.json` —— 移除 csurf
- `backend/package-lock.json` —— 同步
- `backend/scripts/.deploy-cache.json` —— `git rm --cached`，本地保留

### 前端
- `ai-xsread-vue3/src/api/request.js`
- `ai-xsread-vue3/src/views/profile/NotificationsPage.vue`

### 工程化
- `.github/workflows/ci.yml`
- `.gitignore`

## 3. 闸门验证（T-1.8 / T-5.1）

### 3.1 后端测试
```
cd backend && npm test -- --runInBand
```
结果：
```
Test Suites: 42 passed, 42 total
Tests:       2 skipped, 251 passed, 253 total
Snapshots:   0 total
Time:        7.266 s
```
exit 0。覆盖率 statements 45.99% / branches 35.15% / lines 46%（与上次基线持平，未引入新空白）。

### 3.2 前端构建
```
cd ai-xsread-vue3 && npm run build
```
exit 0。Vite 输出 ReadingPage 60.73kb / vue-vendor 102.58kb 等分包，gzip + brotli 双产物正常。

### 3.3 前端单元测试
```
cd ai-xsread-vue3 && npx vitest run --reporter=verbose
```
结果：
```
Test Files  23 passed (23)
     Tests  107 passed (107)
   Duration  6.77s
```
exit 0。

### 3.4 语法检查
```
node -c backend/src/app.js                  → OK
node -c backend/src/middlewares/auth.js     → OK
node -c backend/src/routes/auth.js          → OK
```

## 4. 已知问题与观察

### 4.1 npm audit 11 个漏洞（1 low / 3 moderate / 7 high）
卸载 csurf 后 `npm audit` 报告 backend 仍有 11 个传递依赖漏洞。本次 Sprint 1 范围内未处理，因为：
- 清单第 7 节明示"bcryptjs 2→3 / multer 1→2 / Express 4→5 主版本统一"属于"长期治理"，不在本 Sprint。
- 主版本升级需要单独 PR 与回归测试。
- 建议列入下一 Sprint 的"依赖升级专项"。

### 4.2 `BIGINT UNSIGNED → INT UNSIGNED` 迁移幂等性
T-2.1 / T-2.2 因迁移**尚未在生产执行**，可直接改写。如已在某些开发机执行过旧版（BIGINT），需先 `DROP TABLE user_preferences; DROP TABLE notifications;` 再重跑迁移。AGENTS.md 与清单注释中均已说明。

### 4.3 `backend/scripts/.deploy-cache.json` 仍在工作区
已 `git rm --cached` 取消跟踪，但本地文件保留（避免下次 deploy 全量重传）。`.gitignore` line 35 已忽略其后续修改。

### 4.4 根目录 verify PNG
`verify-01-profile-logged-out.png` / `verify-02-profile-logged-in.png` 在本次会话起始时即已被本地删除（` D` 状态）。`.gitignore` 已加规则忽略未来同名文件，下次 commit 一起落账即可。

## 5. 待执行项（需用户参与）

### T-2.3 个人中心 5 条链路浏览器手测
需要启动 MySQL（确保 `ai_xsread` 库存在）+ `cd backend && npm run dev` + `cd ai-xsread-vue3 && npm run dev`，然后按清单 488-494 行的 5 条链路在浏览器中点测：
1. 编辑资料 → 改昵称 → 保存
2. 头像上传
3. 修改密码
4. 阅读偏好（拖字号 + 切主题）
5. 通知列表（应至少看到种子欢迎通知 + markRead）

发现 bug 请补到清单底部"待补任务"。

### T-2.4 个人中心 MVP 整组 commit
本次 Sprint 改动 + 个人中心 MVP 未提交改动建议作为**两组独立 commit**入仓：
- **Commit A（本 Sprint）**：清单中 T-1.x / T-3.x 涉及文件 + 迁移修复（T-2.1 / T-2.2）
- **Commit B（个人中心 MVP）**：清单 514-554 行列出的 profile 页 / 组件 / store / 控制器 / 路由 / 测试

也可以按清单原方案合成单一 commit。`AGENTS.md` 禁止自动 commit，需用户人工执行。**不要 `git add .`**，避免拉入 `.qa-test-output/` 之类产物。

### T-4.1 / T-4.2 大文件拆分（推迟为独立 Sprint）
- `ReadingPage.vue` 1030 行 → ≤500 行
- `EnhancedEyeCareMode.vue` 1030 行 → ≤500 行

清单标注"风险最高"，应在独立 PR 中执行并附完整阅读链路回归测试。建议列入 Sprint 2。

### T-5.2 部署 dry-run
```
cd backend
python scripts/deploy.py --frontend --backend --dry-run
```
预期输出文件清单**不应当包含** `.env` / `node_modules` / `verify-*.png` / `.qa-test-output/`。请用户在本机执行后将输出归档至下次发布日志。

## 6. 安全增益总结

本 Sprint 完成后用户端安全姿态变化：

| 项 | 改前 | 改后 |
|---|---|---|
| 后端 token 日志 | 每请求打印前 20 字符 + userId/username | 完全无日志 |
| 前端 token 日志 | DEV 模式打印 tokenPreview + authHeader 前 30 字符 | DEV 仅打印 method + url |
| /api-docs | 生产环境裸暴露 | 生产环境 404 |
| 全局限流 | 仅 production 启用 | 全环境启用（dev 3000/15min） |
| 登录 / 注册限流 | 仅全局桶 | 专项 10/15min 桶 |
| JWT algorithm | 默认（隐式 HS256） | 显式锁 HS256，杜绝 `none` 攻击 |
| csurf 死代码 | 占位但未挂载 | 删除 + 移除 12 个传递依赖 |
| CI 测试闸门 | continue-on-error 假绿 | 失败必红 |
| 外链 referrer | NotificationsPage 仅 noopener | noopener + noreferrer |

## 7. 下一步建议

1. **先做 T-2.3 / T-2.4**：把本 Sprint 改动与个人中心 MVP 一起入仓，然后才能真正"对外候选发布用户端"。
2. **管理端 Sprint**：参考 `2026-05-20-管理端优化任务执行清单.md` 单独排期。SQL 注入 / 默认密码 / 构建失败属阻塞级。
3. **依赖升级专项**：`bcryptjs 2→3` / `multer 1→2` / `Express 4→5` 三个主版本统一 + npm audit 7 high 处理。
4. **大文件治理 Sprint**：T-4.1 ReadingPage + T-4.2 EnhancedEyeCareMode，单独 PR 并附阅读链路 e2e 回归。

---

**完成判据（用户端 Sprint 1）**：本报告 §0 表格中**所有自动化判据已绿**。剩余 4 条人工判据（手测、commit、git status 干净、deploy dry-run）依赖用户本地环境执行后即可宣告 Sprint 1 完成。
