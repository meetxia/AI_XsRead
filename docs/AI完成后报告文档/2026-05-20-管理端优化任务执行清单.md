# 管理端优化任务执行清单（Sprint 2）

文档日期：2026-05-20
范围：管理前端 `admin-frontend/` + 管理后端 `admin-backend/` + 关联数据库迁移 + Nginx 配置（管理端入口）
来源：审查报告 03 / 04 / 05 / 06 + 负责人总报告
不在范围：用户前端 / 用户后端（已在 [用户端优化清单](2026-05-20-用户端优化任务执行清单.md) 中）

> ⚠ **重要前提**：建议先把用户端 Sprint 1 阶段 1（恢复闸门）跑完再开始本清单。原因：本清单阶段 5 要改 `nginx.conf`，会同时影响用户前端的 admin/api 反代——如果用户后端测试都还没绿，nginx 切换时不好定位问题。

---

## 0. 使用说明

本清单按"先解除攻击链 → 收口数据库 → 加固上传与错误处理 → 改 Nginx → 补测试与缺失功能 → 大文件治理"的顺序排。

每个任务都给：
- **目标**：完成判据
- **改哪里**：精确到 `文件:行号`
- **改成什么**：可直接复用的 patch
- **怎么验证**：可执行命令 + 预期输出
- **预估**：单人工时
- **依赖**：必须先做完哪些任务

任务编号 `T-X.Y` = 阶段 X 第 Y 个任务。

---

## 阶段 1 — 解除"管理端攻击链"+ 修构建闸门（半天，必做）

> 目标：把"默认密码 + SQL 注入 + 限流失效 + 构建失败"这条 Critical 金线一次性解除。
> 完成判据：`admin-frontend && npm run build` 退出 0；管理后端启动后限流真正生效；novelController 不再可注入；上传路由不再 ReferenceError。

### [ ] T-1.1 — 修 Settings ASI bug 让管理前端构建通过（10 分钟）

**目标**：`admin-frontend npm run build` 退出 0。

**改哪里**：`admin-frontend/src/views/Settings/index.vue:268-276` 和 `:367-371`（两处相同 bug）

**根因**：上一行 `if (!value) return cb()` 没分号 + 下一行 `/^\d{5,15}$/` 行首正则被 babel 当成除法。

**改成什么**：
```js
// 第 1 处（contact_qq，line 268-276）
// 当前：
contact_qq: [
  {
    validator: (_rule, value, cb) => {
      if (!value) return cb()
      /^\d{5,15}$/.test(value) ? cb() : cb(new Error('QQ 应为 5-15 位数字'))
    },
    trigger: 'blur'
  }
],

// 改成：
contact_qq: [
  {
    validator: (_rule, value, cb) => {
      if (!value) { cb(); return; }
      const ok = /^\d{5,15}$/.test(value)
      ok ? cb() : cb(new Error('QQ 应为 5-15 位数字'))
    },
    trigger: 'blur'
  }
],
```

**第 2 处（line 367-371，contactForm.qq 校验器，同样写法）** 用同样手法改。

**怎么验证**：
```bash
cd admin-frontend
npm run build 2>&1 | tail -20
```
预期：看到 `✓ built in ...`，退出码 0。

**依赖**：无。

---

### [ ] T-1.2 — 修 admin-backend `routes/upload.js` ReferenceError（2 分钟）

**目标**：上传错误处理不再撞 `multer is not defined`。

**改哪里**：`admin-backend/src/routes/upload.js:1-7`（顶部 import）

**改成什么**：
```js
// 当前：
const express = require('express');
const router = express.Router();
const upload = require('../services/uploadService');
const { authMiddleware } = require('../middlewares/auth');
const Response = require('../utils/response');

// 改成：补 multer import
const express = require('express');
const multer = require('multer');               // ← 新增
const router = express.Router();
const upload = require('../services/uploadService');
const { authMiddleware } = require('../middlewares/auth');
const Response = require('../utils/response');
```

**怎么验证**：
```bash
cd admin-backend
node -c src/routes/upload.js
```
预期：无输出。

**依赖**：无。

---

### [ ] T-1.3 — 修 admin novelController ORDER BY SQL 注入（10 分钟）

**目标**：`/api/admin/novels?sort=...` 不再可注入。

**改哪里**：`admin-backend/src/controllers/novelController.js:11-67`（getList 方法整段）

**改成什么**：
```js
// 在文件顶部加白名单常量（class 上面）
const ALLOWED_SORTS = new Set([
  'last_update_time',
  'created_at',
  'updated_at',
  'views',
  'likes',
  'collections',
  'rating',
  'word_count'
]);

class NovelController {
  static async getList(req, res, next) {
    try {
      const {
        page = 1,
        pageSize = 20,
        category,
        status,
        keyword,
        sort = 'last_update_time'
      } = req.query;

      // 白名单收敛 sort，未在白名单的回退到默认
      const sortField = ALLOWED_SORTS.has(String(sort)) ? sort : 'last_update_time';

      const offset = (parseInt(page) - 1) * parseInt(pageSize);

      let whereClause = '1=1';
      const params = [];

      if (category) {
        whereClause += ' AND n.category_id = ?';
        params.push(category);
      }
      if (status !== undefined && status !== '') {
        whereClause += ' AND n.status = ?';
        params.push(parseInt(status));
      }
      if (keyword) {
        whereClause += ' AND (n.title LIKE ? OR n.author LIKE ?)';
        params.push(`%${keyword}%`, `%${keyword}%`);
      }

      const [countResult] = await db.query(
        `SELECT COUNT(*) as total FROM novels n WHERE ${whereClause}`,
        params
      );
      const total = countResult[0].total;

      const [novels] = await db.query(
        `SELECT n.*, c.name as category
         FROM novels n
         LEFT JOIN categories c ON n.category_id = c.id
         WHERE ${whereClause}
         ORDER BY n.${sortField} DESC
         LIMIT ? OFFSET ?`,
        [...params, parseInt(pageSize), offset]
      );

      return Response.page(res, novels, total, parseInt(page), parseInt(pageSize));
    } catch (error) {
      next(error);
    }
  }
  // ...其余方法不动
}
```

**怎么验证**：
```bash
cd admin-backend
node -c src/controllers/novelController.js
# 启服务后试一发非法 sort
NODE_ENV=development npm run dev &
sleep 3
curl -s 'http://localhost:8001/api/admin/novels?sort=id;DROP%20TABLE' \
  -H 'Authorization: Bearer FAKE' | head
kill %1
```
预期：401（认证失败，但表明 sort 参数没让 SQL 出错；改前会撞 syntax error 500）。

**依赖**：无。

---

### [ ] T-1.4 — admin-backend 加 `trust proxy`（1 分钟）

**目标**：让管理后端的限流（5/15min 登录 + 100/15min 全局）在 nginx 反代后真正按 IP 分桶生效。

**改哪里**：`admin-backend/src/app.js:14-17`（紧跟 `app.use(helmet())` 之后）

**改成什么**：
```js
// 当前：
const app = express();

// ===== 安全中间件 =====
app.use(helmet());

// ===== CORS配置 =====

// 改成：
const app = express();

// ===== 安全中间件 =====
app.use(helmet());

// nginx 反代后信任最近一跳代理，否则 req.ip 永远是 127.0.0.1，限流会全站共享一桶
app.set('trust proxy', 1);

// ===== CORS配置 =====
```

**怎么验证**：
```bash
cd admin-backend
node -c src/app.js
grep -n "trust proxy" src/app.js
```
预期：能看到新加的那行。

**依赖**：无。

---

### [ ] T-1.5 — admin-frontend `request.js` 修响应码严格判等（5 分钟）

**目标**：管理后端返回 `code: 201`（如 NovelController.create）不再被前端误判为错误。

**改哪里**：`admin-frontend/src/utils/request.js:31-48`

**改成什么**：
```js
// 当前：
service.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')
      if (res.code === 401) {
        ElMessage.error('登录已过期，请重新登录')
        localStorage.removeItem('admin_token')
        localStorage.removeItem('admin_user')
        router.push('/login')
      }
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    return res
  },

// 改成：接受 200-299 区间
service.interceptors.response.use(
  response => {
    const res = response.data
    const isSuccess = res.code === undefined || (res.code >= 200 && res.code < 300)
    if (!isSuccess) {
      ElMessage.error(res.message || '请求失败')
      if (res.code === 401) {
        ElMessage.error('登录已过期，请重新登录')
        localStorage.removeItem('admin_token')
        localStorage.removeItem('admin_user')
        router.push('/login')
      }
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    return res
  },
```

**怎么验证**：
```bash
cd admin-frontend
npm run build 2>&1 | tail -5
```

**依赖**：T-1.1（构建得先能跑）。

---

### [ ] T-1.6 — admin-backend 加 compression + testConnection（10 分钟）

**目标**：dashboard 大 JSON 自动压缩；启动期撞不到 DB 直接 exit 1（不要把烂坏状态推给运行期）。

**改哪里**：`admin-backend/src/app.js:1-14` 顶部 + `:72-85` 启动段

**改成什么**：
```js
// 顶部：
const express = require('express');
const cors = require('cors');
const helmet = require('helmet');
const compression = require('compression');     // ← 新增
const rateLimit = require('express-rate-limit');
const path = require('path');
require('dotenv').config();

const config = require('./config');
const db = require('./config/database');         // ← 新增
const routes = require('./routes');
const { errorHandler, notFoundHandler } = require('./middlewares/errorHandler');
const { devLogger } = require('./middlewares/logger');

// 在 helmet 后、cors 前插入：
app.use(helmet());
app.set('trust proxy', 1);
app.use(compression());                          // ← 新增
app.use(cors({ origin: config.cors.origin, credentials: true }));

// 把启动段（第 72 行起）替换为：
const PORT = config.port;

async function startServer() {
  // 启动期数据库探测：撞不到直接 exit
  try {
    if (typeof db.query === 'function') {
      await db.query('SELECT 1');
    }
  } catch (err) {
    console.error('❌ 数据库连接失败，管理后端启动中止:', err.message);
    process.exit(1);
  }

  const server = app.listen(PORT, () => {
    console.log('');
    console.log('========================================');
    console.log('  🚀 MOMO小说后台管理API服务');
    console.log('========================================');
    console.log(`  环境: ${config.env}`);
    console.log(`  端口: ${PORT}`);
    console.log(`  地址: http://localhost:${PORT}`);
    console.log('========================================');
    console.log('');
  });

  process.on('SIGTERM', () => {
    console.log('收到SIGTERM信号，开始优雅关闭...');
    server.close(() => {
      console.log('服务器已关闭');
      process.exit(0);
    });
  });

  process.on('SIGINT', () => {
    console.log('\n收到SIGINT信号，开始优雅关闭...');
    server.close(() => process.exit(0));
  });
}

if (require.main === module) {
  startServer();
}

module.exports = app;
```

**怎么验证**：
```bash
cd admin-backend
node -c src/app.js
```

**依赖**：T-1.4（trust proxy 也在 app.js 改）。

---

### [ ] T-1.7 — 阶段 1 收尾（10 分钟）

```bash
cd admin-frontend
npm run build 2>&1 | tail -10        # 必须 exit 0

cd ../admin-backend
node -c src/app.js                    # 必须无输出

# 启服务自检
NODE_ENV=development npm run dev &
sleep 3
curl -s http://localhost:8001/api/health
kill %1
```

**预期**：build 退出 0、语法检查通过、health 返回 200。

---

## 阶段 2 — 解除"默认密码 + JWT 单密钥"链路（半天）

> 目标：让 admin/admin123 + 单 JWT secret 这条软肋一次性补上。
> 完成判据：管理前端 Login 页不再预填默认值；init-admin.js 默认密码每次随机；JWT access 与 refresh 用独立密钥并显式 HS256。

### [ ] T-2.1 — 删除 admin Login 默认值与文案（5 分钟）

**目标**：登录页不再"一键登入默认管理员"。

**改哪里**：`admin-frontend/src/views/Login/index.vue:51-54, 71-74`

**改成什么**：
```vue
<!-- 当前 51-54 行：-->
<div class="login-footer">
  <p>默认账号: admin / 密码: admin123</p>
</div>

<!-- 改成：删除整段 footer 或换成中性提示 -->
<div class="login-footer">
  <p>仅限授权管理员登录</p>
</div>
```

```js
// 当前 71-74 行：
const loginForm = reactive({
  username: 'admin',
  password: 'admin123'
})

// 改成：
const loginForm = reactive({
  username: '',
  password: ''
})
```

**怎么验证**：
```bash
cd admin-frontend
grep -n "admin123\|默认账号" src/views/Login/index.vue
```
预期：无输出。

**依赖**：T-1.1（构建闸门已通）。

---

### [ ] T-2.2 — `init-admin.js` 改随机密码（30 分钟）

**目标**：首次部署生成强随机密码，输出到一次性凭证文件，运维必须读完销毁。

**改哪里**：`admin-backend/scripts/init-admin.js`

**改成什么**：整段重写
```js
/**
 * 初始化默认管理员账号
 * 首次部署使用：node scripts/init-admin.js
 * 输出一次性凭证到 admin-backend/.admin-credentials（运维记录后必须删除）
 */

const bcrypt = require('bcryptjs');
const crypto = require('crypto');
const fs = require('fs');
const path = require('path');
const mysql = require('mysql2/promise');
require('dotenv').config();

function generateStrongPassword(length = 20) {
  // 字符集：大小写 + 数字 + 安全符号（去掉容易混淆的 0/O/I/l/1）
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnpqrstuvwxyz23456789#@$%&*+-_';
  let pwd = '';
  const bytes = crypto.randomBytes(length);
  for (let i = 0; i < length; i++) {
    pwd += chars[bytes[i] % chars.length];
  }
  return pwd;
}

async function initAdmin() {
  const requiredEnvVars = ['DB_HOST', 'DB_USER', 'DB_PASSWORD', 'DB_NAME'];
  const missingVars = requiredEnvVars.filter(v => !process.env[v]);
  if (missingVars.length > 0) {
    console.error('❌ 缺少必需的环境变量:', missingVars.join(', '));
    process.exit(1);
  }

  const connection = await mysql.createConnection({
    host: process.env.DB_HOST,
    port: process.env.DB_PORT || 3306,
    user: process.env.DB_USER,
    password: process.env.DB_PASSWORD,
    database: process.env.DB_NAME
  });

  try {
    const [existing] = await connection.query(
      'SELECT id FROM admin_users WHERE username = ?',
      ['admin']
    );

    if (existing.length > 0) {
      console.log('⚠️  admin 账号已存在，本脚本不重置已有密码。');
      console.log('   如需重置，请手工执行 SQL 或运行 scripts/reset-admin-password.js');
      process.exit(0);
    }

    const password = generateStrongPassword(20);
    const hashed = await bcrypt.hash(password, 12);  // 12 rounds，比之前 10 更稳

    await connection.query(
      `INSERT INTO admin_users (username, password, email, role)
       VALUES (?, ?, ?, ?)`,
      ['admin', hashed, 'admin@example.com', 'super_admin']
    );

    // 写一次性凭证文件
    const credPath = path.join(__dirname, '..', '.admin-credentials');
    const content = [
      '# MOMO 小说管理后台首次凭证（一次性，请记录后立刻删除此文件）',
      `# 生成时间: ${new Date().toISOString()}`,
      '',
      'username: admin',
      `password: ${password}`,
      '',
      '# 登录后请立刻在 /settings 修改密码',
      ''
    ].join('\n');
    fs.writeFileSync(credPath, content, { mode: 0o600 });

    console.log('✅ 默认管理员账号创建成功');
    console.log(`   凭证已写入：${credPath}`);
    console.log('   请运维立刻读取该文件并安全销毁，不要留存到 git / 聊天记录 / 邮件');
  } finally {
    await connection.end();
  }
}

initAdmin().catch(err => {
  console.error('❌ 初始化失败:', err.message);
  process.exit(1);
});
```

**配套**：在 `.gitignore` 末尾追加
```
admin-backend/.admin-credentials
```

**怎么验证**：
```bash
cd admin-backend
# 在干净 DB 上跑（先删 admin 行）
mysql -u <user> -p ai_xsread -e "DELETE FROM admin_users WHERE username='admin';"
node scripts/init-admin.js
cat .admin-credentials
```
预期：看到 20 位随机密码；文件权限 600；用此密码可登录。

**依赖**：无。

---

### [ ] T-2.3 — JWT 拆双密钥 + 显式 HS256（20 分钟）

**目标**：admin-backend 的 access 与 refresh 用独立 secret；显式锁定 algorithm 防 none 攻击。

**改哪里**：3 个文件

**改 1：`admin-backend/.env.example`**
```bash
# 当前：
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long_change_in_production
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

# 改成：补 JWT_REFRESH_SECRET
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long_change_in_production
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_at_least_32_characters_long_change_in_production
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d
```

**改 2：`admin-backend/src/config/index.js:14-23`**
```js
// 在 validateEnv() 里追加 JWT_REFRESH_SECRET 校验：
if (!process.env.JWT_REFRESH_SECRET) {
  errors.push('JWT_REFRESH_SECRET (必需)');
} else if (process.env.JWT_REFRESH_SECRET.length < 32) {
  console.error('❌ JWT_REFRESH_SECRET 长度必须至少32个字符');
  console.error(`   当前长度: ${process.env.JWT_REFRESH_SECRET.length} 字符`);
  process.exit(1);
}

// 修改 jwt 导出：
jwt: {
  secret: process.env.JWT_SECRET,
  refreshSecret: process.env.JWT_REFRESH_SECRET,   // ← 新增
  expiresIn: process.env.JWT_EXPIRES_IN || '2h',
  refreshExpiresIn: process.env.JWT_REFRESH_EXPIRES_IN || '7d'
},
```

**改 3：`admin-backend/src/utils/jwt.js`**
```js
const jwt = require('jsonwebtoken');
const config = require('../config');

class JWTUtil {
  /** access token：用 jwt.secret + 显式 HS256 */
  static generateAccessToken(payload) {
    return jwt.sign(payload, config.jwt.secret, {
      expiresIn: config.jwt.expiresIn,
      algorithm: 'HS256'
    });
  }

  /** refresh token：用独立 jwt.refreshSecret */
  static generateRefreshToken(payload) {
    return jwt.sign(payload, config.jwt.refreshSecret, {
      expiresIn: config.jwt.refreshExpiresIn,
      algorithm: 'HS256'
    });
  }

  /** 验证 access token */
  static verifyToken(token) {
    return jwt.verify(token, config.jwt.secret, { algorithms: ['HS256'] });
  }

  /** 验证 refresh token */
  static verifyRefreshToken(token) {
    return jwt.verify(token, config.jwt.refreshSecret, { algorithms: ['HS256'] });
  }

  static decodeToken(token) {
    return jwt.decode(token);
  }
}

module.exports = JWTUtil;
```

**改 4：`admin-backend/src/controllers/authController.js:140-145`**（refreshToken 方法用新方法）
```js
// 当前（line 141）：
const decoded = JWTUtil.verifyToken(refreshToken);

// 改成：
const decoded = JWTUtil.verifyRefreshToken(refreshToken);
```

**怎么验证**：
```bash
cd admin-backend
# 1. 在 .env 里加 JWT_REFRESH_SECRET
node -c src/app.js
# 2. 缺密钥时是否真的 exit 1
unset JWT_REFRESH_SECRET
node -e "require('./src/config')" 2>&1 | head -5
# 预期：看到 ❌ 缺少必需的环境变量 提示
```

**依赖**：无。

---

## 阶段 3 — 数据库与迁移收口（半天）

> 目标：让管理端 5 张表进入 migrate 流水线；统一双后端 DB 变量；处理 banners/announcements 空壳表。
> 完成判据：新机器执行 `cd backend && npm start` 一次能把所有表（含管理表）建出来；admin-backend 不再依赖 `scripts/create-admin-tables.js`。

### [ ] T-3.1 — 5 张管理表迁入 migrate 流水线（1 小时）

**目标**：admin_users / admin_logs / banners / announcements / statistics_daily 走统一迁移引擎。

**改哪里**：新增迁移文件 + 弃用旧脚本

**步骤 1**：新建 `backend/database/migrations/202605200001__add_admin_tables.sql`
```sql
-- 把 admin-backend/scripts/create-admin-tables.js 的 5 张表迁入正式迁移流水线
-- 来源：scripts/create-admin-tables.js（已废弃，新机器部署不再手工执行）

SET NAMES utf8mb4;

-- 1. 管理员账号
CREATE TABLE IF NOT EXISTS admin_users (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '管理员ID',
  username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  password VARCHAR(255) NOT NULL COMMENT '密码（bcrypt）',
  email VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  avatar VARCHAR(255) DEFAULT NULL,
  real_name VARCHAR(50) DEFAULT NULL,
  phone VARCHAR(20) DEFAULT NULL,
  role VARCHAR(20) DEFAULT 'admin' COMMENT 'super_admin / admin / editor',
  status TINYINT DEFAULT 1 COMMENT '0-禁用 1-启用',
  last_login_time DATETIME DEFAULT NULL,
  last_login_ip VARCHAR(64) DEFAULT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员账号';

-- 2. 管理员操作日志
CREATE TABLE IF NOT EXISTS admin_logs (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  admin_id INT UNSIGNED DEFAULT NULL,
  admin_username VARCHAR(50) DEFAULT NULL,
  action VARCHAR(50) NOT NULL,
  module VARCHAR(50) NOT NULL,
  target_id INT UNSIGNED DEFAULT NULL,
  description TEXT DEFAULT NULL,
  ip VARCHAR(64) DEFAULT NULL,
  user_agent TEXT DEFAULT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_admin_id (admin_id),
  INDEX idx_module_action (module, action),
  INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员操作日志';

-- 3. 轮播图（前端首页 banner）
CREATE TABLE IF NOT EXISTS banners (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(100) NOT NULL,
  image VARCHAR(255) NOT NULL,
  link VARCHAR(255) DEFAULT NULL,
  link_type VARCHAR(20) DEFAULT 'novel',
  target_id INT UNSIGNED DEFAULT NULL,
  sort_order INT DEFAULT 0,
  status TINYINT DEFAULT 1 COMMENT '0-隐藏 1-显示',
  start_time DATETIME DEFAULT NULL,
  end_time DATETIME DEFAULT NULL,
  views INT UNSIGNED DEFAULT 0,
  clicks INT UNSIGNED DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_status_sort (status, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='轮播图';

-- 4. 公告
CREATE TABLE IF NOT EXISTS announcements (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(100) NOT NULL,
  content TEXT NOT NULL,
  type VARCHAR(20) DEFAULT 'system',
  position VARCHAR(100) DEFAULT 'home',
  priority TINYINT DEFAULT 0,
  status TINYINT DEFAULT 1,
  start_time DATETIME DEFAULT NULL,
  end_time DATETIME DEFAULT NULL,
  views INT UNSIGNED DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_status_priority (status, priority)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='公告';

-- 5. 每日统计
CREATE TABLE IF NOT EXISTS statistics_daily (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  date DATE NOT NULL UNIQUE,
  new_users INT UNSIGNED DEFAULT 0,
  active_users INT UNSIGNED DEFAULT 0,
  page_views INT UNSIGNED DEFAULT 0,
  unique_visitors INT UNSIGNED DEFAULT 0,
  reading_duration INT UNSIGNED DEFAULT 0 COMMENT '总阅读时长（秒）',
  avg_reading_duration DECIMAL(10,2) DEFAULT 0.00,
  new_comments INT UNSIGNED DEFAULT 0,
  new_collections INT UNSIGNED DEFAULT 0,
  new_likes INT UNSIGNED DEFAULT 0,
  revenue DECIMAL(10,2) DEFAULT 0.00,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_date (date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='每日统计';
```

**步骤 2**：把 `admin-backend/scripts/create-admin-tables.js` 改造成"提示已迁移"的占位脚本（不直接删，避免老 README 失效）。
```js
// admin-backend/scripts/create-admin-tables.js
/**
 * @deprecated 自 2026-05-20 起，管理后台 5 张表已并入用户后端的迁移流水线。
 * 新机器部署只需启动用户后端（cd backend && npm start），它会自动执行
 * backend/database/migrations/202605200001__add_admin_tables.sql。
 *
 * 本脚本保留为提示，运行后会立刻退出。
 */
console.error('⚠️  此脚本已废弃。');
console.error('   管理表已迁入 backend/database/migrations/202605200001__add_admin_tables.sql');
console.error('   请改为：cd backend && npm start（首次启动会自动建表）');
process.exit(1);
```

**步骤 3**：更新 `deploy-guide.md` / `README.md` 中提到 `node scripts/create-admin-tables.js` 的位置。

**怎么验证**：
```bash
# 1. 在干净 DB 上验证
mysql -u <user> -p -e "DROP DATABASE ai_xsread; CREATE DATABASE ai_xsread CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -u <user> -p ai_xsread < backend/database/init_step1.sql

cd backend
NODE_ENV=development npm start &
sleep 5
mysql -u <user> -p ai_xsread -e "SHOW TABLES;" | grep -E "admin_users|admin_logs|banners|announcements|statistics_daily"
kill %1
```
预期：5 张管理表都在；管理后端 `npm run dev` 也能启动。

**依赖**：无（与 T-3.2 可并行）。

---

### [ ] T-3.2 — admin-backend 兼容 `DB_DATABASE` 与 `DB_NAME`（10 分钟）

**目标**：消除"双后端 .env 必须双写"的运维坑。

**改哪里**：`admin-backend/src/config/index.js:11-13` 与数据库 connection 取值

**改成什么**：
```js
// 当前 line 11-13：
if (!process.env.DB_HOST) errors.push('DB_HOST');
if (!process.env.DB_USER) errors.push('DB_USER');
if (!process.env.DB_PASSWORD) errors.push('DB_PASSWORD');
if (!process.env.DB_NAME) errors.push('DB_NAME');

// 改成：双兼容（优先 DB_NAME，回退 DB_DATABASE）
const dbName = process.env.DB_NAME || process.env.DB_DATABASE;
if (!process.env.DB_HOST) errors.push('DB_HOST');
if (!process.env.DB_USER) errors.push('DB_USER');
if (!process.env.DB_PASSWORD) errors.push('DB_PASSWORD');
if (!dbName) errors.push('DB_NAME (或 DB_DATABASE)');

// 在 module.exports 的 database 块也用 dbName：
database: {
  host: process.env.DB_HOST,
  port: process.env.DB_PORT || 3306,
  user: process.env.DB_USER,
  password: process.env.DB_PASSWORD,
  database: dbName,                              // ← 用前面的 dbName
},
```

**注意**：`admin-backend/scripts/init-admin.js`、`scripts/check-*.js` 等脚本里也会用 `process.env.DB_NAME`，需要同步改成双兼容（用 `process.env.DB_NAME || process.env.DB_DATABASE`）。

**改 .env.example**：在 DB_NAME 上方加注释
```bash
# 数据库名（可选别名 DB_DATABASE，与用户后端 backend/.env 保持一致即可）
DB_NAME=ai_xsread
```

**怎么验证**：
```bash
cd admin-backend
# 用 DB_DATABASE 启动
DB_DATABASE=ai_xsread DB_NAME= node -e "console.log(require('./src/config').database.database)"
# 预期：ai_xsread
DB_NAME=ai_xsread node -e "console.log(require('./src/config').database.database)"
# 预期：ai_xsread
```

**依赖**：无。

---

### [ ] T-3.3 — banners / announcements 决策（30 分钟）

**目标**：要么实现 controller，要么承认不做。

**判断**：
- 如果产品确认"短期内不做轮播图 / 公告管理"——保留表（为将来留位置），管理前端不加入口。**记一条 backlog**。
- 如果产品确认"要做"——本 Sprint 不做（避免范围扩散），但在 backlog 单独立项。

**做什么**：在 `docs/项目总文档/` 下新建或更新一份 `2026-05-20-后台功能Backlog.md`，记录：
- banners 表存在但无入口
- announcements 表存在但无入口
- 决策：保留表 / 列入下个 Sprint 实现

不动代码。

**依赖**：无。

---

### [ ] T-3.4 — admin_logs 与 code_admin_logs 双表整合策略（决策项，0 工时）

**目标**：明确"双表共存"是过渡还是长期。

**判断**：
- `admin_logs`：旧表，novel/user/comment 在用
- `code_admin_logs`：新表，code/contact/membershipAdmin 在用

**建议**：双表暂时保留，下一 Sprint 做 SQL UNION VIEW 对前端统一暴露。本清单不动。

记入 §T-3.3 的 backlog。

---

## 阶段 4 — 上传安全 + errorHandler 加固（半天）

### [ ] T-4.1 — 严格 MIME 白名单 + SVG 排除（10 分钟）

**目标**：admin-backend 通用上传不再放过 SVG（避免内嵌 JS XSS）。

**改哪里**：`admin-backend/src/services/uploadService.js:23-31`

**改成什么**：
```js
// 当前：
const fileFilter = (req, file, cb) => {
  if (file.mimetype.startsWith('image/')) {
    cb(null, true);
  } else {
    cb(new Error('只允许上传图片文件'), false);
  }
};

// 改成：严格白名单（与 contactController 一致）
const ALLOWED_MIME = new Set([
  'image/jpeg',
  'image/jpg',
  'image/png',
  'image/webp'
]);

const fileFilter = (req, file, cb) => {
  if (!ALLOWED_MIME.has(file.mimetype)) {
    return cb(new Error('仅支持 jpg / png / webp 格式'), false);
  }
  cb(null, true);
};
```

**怎么验证**：
```bash
cd admin-backend
node -c src/services/uploadService.js
```

**依赖**：无。

---

### [ ] T-4.2 — admin errorHandler 脱敏 + 错误码字典化（30 分钟）

**目标**：错误处理不再 console.error 整个 err 对象（含 stack/sql），并统一错误码。

**改哪里**：`admin-backend/src/middlewares/errorHandler.js`

**改成什么**：
```js
const Response = require('../utils/response');

const ErrorMessage = {
  ER_DUP_ENTRY: '数据已存在',
  JsonWebTokenError: 'Token 无效',
  TokenExpiredError: 'Token 已过期',
  ValidationError: '参数校验失败'
};

function logError(err, req) {
  // 仅打印必要字段，不要 dump 整个 err（避免 SQL/req.body 泄漏）
  const safe = {
    timestamp: new Date().toISOString(),
    method: req.method,
    url: req.originalUrl,
    ip: req.ip,
    userId: req.user?.id || 'anonymous',
    error: {
      name: err.name,
      message: err.message,
      code: err.code,
      // stack 仅 dev 环境
      stack: process.env.NODE_ENV !== 'production' ? err.stack : undefined
    }
  };
  if ((err.statusCode || 500) >= 500) {
    console.error('❌ admin-backend error:', JSON.stringify(safe));
  } else {
    console.warn('⚠️ admin-backend warn:', JSON.stringify(safe));
  }
}

const errorHandler = (err, req, res, next) => {
  logError(err, req);

  // MySQL 唯一键冲突
  if (err.code === 'ER_DUP_ENTRY') {
    return Response.error(res, ErrorMessage.ER_DUP_ENTRY, 400);
  }

  // JWT
  if (err.name === 'JsonWebTokenError') {
    return Response.error(res, ErrorMessage.JsonWebTokenError, 401);
  }
  if (err.name === 'TokenExpiredError') {
    return Response.error(res, ErrorMessage.TokenExpiredError, 401);
  }

  // 参数校验
  if (err.name === 'ValidationError') {
    return Response.error(res, err.message || ErrorMessage.ValidationError, 400);
  }

  // multer
  if (err.name === 'MulterError') {
    if (err.code === 'LIMIT_FILE_SIZE') {
      return Response.error(res, '文件大小超过限制', 400);
    }
    return Response.error(res, '文件上传失败', 400);
  }

  // 默认
  const statusCode = err.statusCode || 500;
  // 生产环境：500 错误对外只返回固定文案
  const isProd = process.env.NODE_ENV === 'production';
  const message = isProd && statusCode >= 500
    ? '服务器内部错误'
    : (err.message || '服务器内部错误');

  return Response.error(res, message, statusCode);
};

const notFoundHandler = (req, res) => {
  return Response.error(res, '请求的资源不存在', 404);
};

module.exports = { errorHandler, notFoundHandler };
```

**怎么验证**：
```bash
cd admin-backend
node -c src/middlewares/errorHandler.js
```

**依赖**：无。

---

## 阶段 5 — Nginx 配置（半天）

> 目标：补 HTTPS / admin 子域 / 安全响应头 / brotli。这是发布前最后一块大砖。
> 完成判据：浏览器访问 https://xs.momofx.cn 自动跳 https；admin.momofx.cn 能进管理前端；F12 网络看到 HSTS / CSP；.br 文件被正确返回。

> ⚠ 强烈建议：先在测试服务器或本地 nginx 跑一遍，再换生产配置。

### [ ] T-5.1 — 改写 `nginx.conf`（半天）

**目标**：改成"用户域 + 管理域 + HTTPS + 安全头 + brotli"。

**改哪里**：`nginx.conf`（整文件重写）

**改成什么**：
```nginx
# Nginx 配置 - MOMO 小说（xs.momofx.cn + admin.xs.momofx.cn）
# 部署前置条件：宝塔已经申请并配置好 SSL 证书路径

# ============================================
# 1. HTTP → HTTPS 跳转（用户域 + 管理域）
# ============================================
server {
    listen 80;
    server_name xs.momofx.cn admin.xs.momofx.cn;
    return 301 https://$host$request_uri;
}

# ============================================
# 2. 用户前端（HTTPS）
# ============================================
server {
    listen 443 ssl http2;
    server_name xs.momofx.cn;

    # SSL（路径以宝塔实际生成为准）
    ssl_certificate     /www/server/panel/vhost/cert/xs.momofx.cn/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/xs.momofx.cn/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5:!RC4:!3DES;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    root /www/wwwroot/xs.momofx.cn/ai-xsread-vue3/dist;
    index index.html;

    access_log /www/wwwlogs/xs.momofx.cn_access.log;
    error_log  /www/wwwlogs/xs.momofx.cn_error.log;

    # ── 安全响应头 ──
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # ── 压缩：gzip + brotli（brotli 需要 nginx 装 ngx_brotli 模块）──
    gzip on;
    gzip_vary on;
    gzip_min_length 256;
    gzip_comp_level 5;
    gzip_types text/plain text/css text/xml text/javascript
               application/javascript application/x-javascript
               application/json application/xml+rss application/xml
               image/svg+xml font/woff2;

    # 如果 nginx 装了 brotli 模块，再开下面两行；否则保持注释
    # brotli on;
    # brotli_types text/plain text/css application/javascript application/json image/svg+xml font/woff2;

    # ── 反代用户后端 API ──
    location /api {
        proxy_pass http://127.0.0.1:8005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # CORS 由后端 cors() 控制，这里不再加 wildcard
    }

    # ── 上传文件 ──
    location /uploads {
        alias /www/wwwroot/xs.momofx.cn/backend/uploads;
        expires 365d;
        add_header Cache-Control "public, max-age=31536000, immutable" always;

        location ~* \.(jpg|jpeg|png)$ {
            add_header Cache-Control "public, max-age=31536000, immutable" always;
            add_header Vary "Accept" always;
            try_files $uri $uri =404;
        }
    }

    # ── 带 hash 的 JS/CSS 长缓存 ──
    location ~* /(?:js|css)/.*\.[a-f0-9]{8,}\.(js|css)$ {
        expires 365d;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    # ── 静态资源 30d ──
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|avif|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000" always;
    }

    # ── HTML 不缓存 ──
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    # ── SPA fallback ──
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── 屏蔽隐藏文件与 stats.html ──
    location ~ /\. { deny all; }
    location = /stats.html { deny all; }
}

# ============================================
# 3. 管理前端（HTTPS，子域）
# ============================================
server {
    listen 443 ssl http2;
    server_name admin.xs.momofx.cn;

    ssl_certificate     /www/server/panel/vhost/cert/admin.xs.momofx.cn/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/admin.xs.momofx.cn/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5:!RC4:!3DES;

    root /www/wwwroot/xs.momofx.cn/admin-frontend/dist;
    index index.html;

    access_log /www/wwwlogs/admin.xs.momofx.cn_access.log;
    error_log  /www/wwwlogs/admin.xs.momofx.cn_error.log;

    # 管理后台更严格的安全头
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin" always;

    gzip on;
    gzip_types text/plain text/css application/javascript application/json;

    # 反代管理后端
    location /api {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ~* /(?:js|css)/.*\.[a-f0-9]{8,}\.(js|css)$ {
        expires 365d;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 屏蔽
    location ~ /\. { deny all; }
    location = /stats.html { deny all; }
}
```

**配套**：
1. 域名解析：`admin.xs.momofx.cn` 加 A 记录指向同 IP
2. 宝塔 / acme 申请 admin 子域 SSL 证书
3. 同步更新 `deploy-guide.md` 的 §6（管理端访问入口）

**怎么验证**：
```bash
# 服务器上：
nginx -t
# 预期：syntax is ok / test is successful
nginx -s reload

# 客户端：
curl -sI http://xs.momofx.cn | head -5
# 预期：HTTP/1.1 301 Moved Permanently → https://xs.momofx.cn

curl -sI https://admin.xs.momofx.cn | head -10
# 预期：200 + Strict-Transport-Security / X-Frame-Options
```

**依赖**：T-1.1（管理前端构建必须先能跑）；T-2.1（不能在公网上线一个默认 admin/admin123 文案的 Login 页）。

---

## 阶段 6 — 测试与缺失功能（1.5 天）

### [ ] T-6.1 — admin-backend 引入 jest + supertest 最小测试（半天）

**目标**：引入测试框架，写 3 条 smoke 让 CI 卡门起作用。

**改哪里**：`admin-backend/package.json` + 新增 `tests/`

**步骤 1：装依赖**
```bash
cd admin-backend
npm install --save-dev jest supertest @types/jest
```

**步骤 2：改 package.json**
```json
"scripts": {
  "start": "node src/app.js",
  "dev": "nodemon src/app.js",
  "test": "jest --runInBand --testTimeout=10000"
},
"jest": {
  "testEnvironment": "node",
  "testMatch": ["**/tests/**/*.test.js"]
}
```

**步骤 3：写 3 条 smoke**

`admin-backend/tests/auth.test.js`：
```js
const request = require('supertest');
process.env.NODE_ENV = 'test';
process.env.JWT_SECRET = process.env.JWT_SECRET || 'a'.repeat(40);
process.env.JWT_REFRESH_SECRET = process.env.JWT_REFRESH_SECRET || 'b'.repeat(40);
process.env.DB_HOST = process.env.DB_HOST || '127.0.0.1';
process.env.DB_USER = process.env.DB_USER || 'root';
process.env.DB_PASSWORD = process.env.DB_PASSWORD || 'root';
process.env.DB_NAME = process.env.DB_NAME || 'ai_xsread_test';

const app = require('../src/app');

describe('admin-backend / auth (smoke)', () => {
  test('POST /api/admin/login 缺密码返回 400', async () => {
    const res = await request(app).post('/api/admin/login').send({ username: 'admin' });
    expect(res.status).toBe(400);
  });

  test('GET /api/admin/profile 无 token 返回 401', async () => {
    const res = await request(app).get('/api/admin/profile');
    expect(res.status).toBe(401);
  });
});
```

`admin-backend/tests/security.test.js`：
```js
const request = require('supertest');
const app = require('../src/app');

describe('admin-backend / security (smoke)', () => {
  test('GET /api/admin/novels?sort=invalid 不会撞 SQL 错', async () => {
    // 即使没 token，也应该被 401 截住而不是 500
    const res = await request(app).get('/api/admin/novels?sort=id;DROP%20TABLE');
    expect([401, 500]).toContain(res.status);
    if (res.status === 500) {
      // 如果是 500，确保不是因为 SQL syntax error
      expect(res.body?.message || '').not.toMatch(/SQL syntax/i);
    }
  });

  test('POST /api/admin/login 限流：超过 5 次返回 429', async () => {
    let last = 0;
    for (let i = 0; i < 7; i++) {
      const r = await request(app).post('/api/admin/login').send({ username: 'wrong', password: 'wrong' });
      last = r.status;
    }
    expect([401, 429]).toContain(last);
  });
});
```

**步骤 4：CI 中改 admin-backend job 跑测试**（已在阶段 7 处理 CI）

**怎么验证**：
```bash
cd admin-backend
npm test 2>&1 | tail -10
```
预期：2 个 suite 全绿（DB 不可达时只有 401 这两条会过）。

**依赖**：阶段 1（trust proxy / SQL 注入修了之后，security 测试才会按预期通过）。

---

### [ ] T-6.2 — admin-frontend 引入 vitest 最小测试（30 分钟）

**目标**：起码有 2 条单元测试守住 router guard + Login 字段空。

**改哪里**：`admin-frontend/package.json` + 新增 `src/__tests__/`

**步骤 1：装依赖**
```bash
cd admin-frontend
npm install --save-dev vitest @vue/test-utils jsdom
```

**步骤 2：改 package.json**
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "test": "vitest --run"
}
```

**步骤 3：补 `vite.config.js` 的 test 配置**（如已有 vitest 配置则跳过）
```js
test: {
  environment: 'jsdom',
  globals: true
}
```

**步骤 4：`src/__tests__/login.test.js`**
```js
import { describe, it, expect } from 'vitest'
import fs from 'fs'
import path from 'path'

describe('Login page hardening', () => {
  it('Login.vue 不再预填 admin/admin123', () => {
    const file = fs.readFileSync(
      path.resolve(__dirname, '../views/Login/index.vue'),
      'utf-8'
    )
    expect(file).not.toMatch(/username:\s*['"]admin['"]/)
    expect(file).not.toMatch(/password:\s*['"]admin123['"]/)
    expect(file).not.toMatch(/默认账号.*admin123/)
  })
})
```

**怎么验证**：
```bash
cd admin-frontend
npm test 2>&1 | tail -10
```

**依赖**：T-2.1（必须先把默认值删了，测试才会绿）。

---

### [ ] T-6.3 — 实现 `/codes` 激活码管理页（1 天，可选）

**目标**：管理后端 codeController 553 行的能力暴露给前端。

**判断**：如果短期内没人手工生成激活码（运营靠脚本走），**列入 backlog 不做**；如果产品需要管理后台界面，本任务展开如下：

**改哪里**：
- `admin-frontend/src/api/code.js` 新增
- `admin-frontend/src/views/Codes/` 新目录（4 个文件：BatchList/BatchDetail/CreateBatch/RedemptionLogs）
- `admin-frontend/src/router/index.js` 加 4 条路由

**展开方式**：略（这个任务**强烈建议作为独立 Sprint 项**）。

如果这个 Sprint 不做，请记 backlog。

**依赖**：阶段 1 / 2（基础设施先稳）。

---

### [ ] T-6.4 — `NovelList.vue` 分类改接口加载（30 分钟）

**目标**：消除分类硬编码（前端写死 4 个 vs 后端 6 个）。

**改哪里**：`admin-frontend/src/views/Content/NovelList.vue:13-22`

**改成什么**：
```vue
<!-- 当前：4 个硬编码 option -->
<el-form-item label="分类">
  <el-select v-model="filters.category" placeholder="全部分类" clearable>
    <el-option label="全部" value="" />
    <el-option label="都市言情" value="101" />
    <el-option label="古风穿越" value="102" />
    <el-option label="悬疑推理" value="103" />
    <el-option label="治愈系" value="104" />
  </el-select>
</el-form-item>

<!-- 改成：从接口加载 -->
<el-form-item label="分类">
  <el-select v-model="filters.category" placeholder="全部分类" clearable>
    <el-option label="全部" value="" />
    <el-option
      v-for="cat in categoryOptions"
      :key="cat.id"
      :label="cat.name"
      :value="cat.id"
    />
  </el-select>
</el-form-item>
```

```js
// 在 <script setup> 中：
import { ref, onMounted } from 'vue'
import { getCategories } from '@/api/novel'   // 如果没有就在 novel.js 加一个
// ...
const categoryOptions = ref([])

onMounted(async () => {
  try {
    const res = await getCategories()
    categoryOptions.value = res.data || []
  } catch (e) {
    console.warn('加载分类失败', e)
  }
})
```

**配套**：管理后端如果还没 `/api/admin/novels/categories`（或公共 `/api/novels/categories`）端点，可以让前端直接用用户后端的公开端点。

**怎么验证**：
```bash
cd admin-frontend && npm run build
```

**依赖**：T-1.1（构建闸门已通）。

---

### [ ] T-6.5 — 修构建产物路径异常（30 分钟）

**目标**：`dist/E:/momo-ruanjiansheji/...` 路径不再出现，stats.html 不进 dist。

**改哪里**：`admin-frontend/vite.config.js`

**改成什么**：
- `vite-plugin-compression` 配置中加 `filename` 或干脆移除——让 Vite 默认走原生 brotli/gzip
- `rollup-plugin-visualizer` 输出路径改到 `dist` 之外，例如 `node_modules/.vite/stats.html`

具体配置（按当前 vite.config.js 实际情况调整）：
```js
import { visualizer } from 'rollup-plugin-visualizer'
import viteCompression from 'vite-plugin-compression'

// plugins:
viteCompression({
  algorithm: 'gzip',
  ext: '.gz',
  threshold: 10240,
  // 显式指定 filename，避免 Windows 下绝对路径泄漏
  filename: '[path][base].gz',
  deleteOriginFile: false
}),
viteCompression({
  algorithm: 'brotliCompress',
  ext: '.br',
  threshold: 10240,
  filename: '[path][base].br',
  deleteOriginFile: false
}),
visualizer({
  // 输出到 dist 之外
  filename: 'node_modules/.vite/stats.html',
  open: false,
  gzipSize: true,
  brotliSize: true
})
```

**怎么验证**：
```bash
cd admin-frontend
rm -rf dist
npm run build
ls dist/js/ | head -10            # 不应再有 E:/... 前缀
ls dist/stats.html 2>&1            # 应该 No such file
```

**依赖**：T-1.1。

---

## 阶段 7 — CI 与大文件治理（半天）

### [ ] T-7.1 — CI 给 admin 两侧加上真实测试（10 分钟）

**目标**：让 CI 不再 continue-on-error 放过 admin 端。

**改哪里**：`.github/workflows/ci.yml` 的 admin-frontend / admin-backend job

**改成什么**：
```yaml
  admin-frontend:
    name: Admin Frontend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: admin-frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm install
      - name: Build
        run: npm run build
      - name: Vitest
        run: npm test                 # 不再 continue-on-error

  admin-backend:
    name: Admin Backend Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: admin-backend
    env:
      NODE_ENV: test
      JWT_SECRET: ci-test-secret-must-be-long-enough-32+chars
      JWT_REFRESH_SECRET: ci-refresh-secret-must-be-long-enough-32+
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: ai_xsread_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm install
      - name: Syntax check
        run: node -c src/app.js
      - name: Jest tests
        run: npm test                 # 不再 continue-on-error
```

**怎么验证**：本地无法直接验证，push 到 PR 看 CI 跑。

**依赖**：T-6.1 / T-6.2（必须先有测试文件）。

---

### [ ] T-7.2 — 拆 `codeController.js`（半天，可选）

**目标**：553 行 → 4 个文件，每个 ≤ 200 行。

**改哪里**：`admin-backend/src/controllers/codeController.js`

**做什么**（建议拆分方向）：
1. `controllers/code/batchController.js` — createBatch / getBatches / getBatchDetail
2. `controllers/code/exportController.js` — exportBatch CSV
3. `controllers/code/voidController.js` — voidCodes
4. `controllers/code/redemptionController.js` — getRedemptions

`routes/codes.js` 同步调整 import。

**注意**：拆完必须确认 `code_admin_logs` 仍然在每个写操作里被调用一次（不要遗漏）。

**怎么验证**：
```bash
cd admin-backend
npm test
# 启服务用 Postman / curl 跑一遍批次 CRUD
```

**依赖**：T-6.1（最好测试先写到位再拆）。

---

### [ ] T-7.3 — 拆 `Settings/index.vue`（半天，可选）

**目标**：536 行 → 3 个 tab 子组件 + 1 个壳。

**改哪里**：`admin-frontend/src/views/Settings/index.vue`

**做什么**：
1. `views/Settings/BasicTab.vue` — 基础设置 tab
2. `views/Settings/ContactTab.vue` — 客服信息 tab
3. `views/Settings/ConfigsTab.vue` — system_config 表 tab
4. `views/Settings/index.vue` 缩成壳（el-tabs + 三个子组件）

**怎么验证**：
```bash
cd admin-frontend
npm run build
# 浏览器手测：三 tab 切换、保存、上传 QR
```

**依赖**：T-1.1（现状先得能 build）。

---

## 阶段 8 — 收尾验证 + 部署演练（半天）

### [ ] T-8.1 — 全量构建 + 测试（30 分钟）

```bash
# 4 子工程
cd backend && npm test -- --runInBand 2>&1 | tail -10
cd ../ai-xsread-vue3 && npm run build && npm test 2>&1 | tail -5
cd ../admin-frontend && npm run build && npm test 2>&1 | tail -5
cd ../admin-backend && npm test 2>&1 | tail -5
```

**预期**：4 子工程全绿。

---

### [ ] T-8.2 — 部署 dry-run（10 分钟）

```bash
cd backend
python scripts/deploy.py --all --dry-run
```

**预期**：列出 4 子工程会上传的文件清单；不应包含 `.env` / `.admin-credentials` / `node_modules` / `verify-*.png` / `.qa-test-output/`。

---

### [ ] T-8.3 — 测试服务器演练（1 小时，强烈推荐）

如果有 staging 环境：
1. 在 staging 跑 `python scripts/deploy.py --all` 真部署
2. 验证：
   - `https://staging-domain/api/health` → 200
   - `https://admin.staging-domain/` → 加载管理前端
   - `nginx -t` 无报错
   - `pm2 status` 两个进程 online
3. 用 init-admin.js 生成的随机密码登录管理后台
4. 跑一遍：
   - 看板 4 个统计卡有数据
   - 小说列表 / 编辑 / 删除
   - 用户列表
   - 评论审核
   - 设置三 tab 都能保存
   - 客服 QR 上传
   - 激活码生成 / 作废 / 兑换记录

**预期**：全链路通。

---

### [ ] T-8.4 — 写完成报告（30 分钟）

按 AGENTS.md §"文档规则"，在 `docs/AI完成后报告文档/` 写：
`2026-05-XX-管理端Sprint2完成报告.md`

至少包含：
- 完成的任务（对照本清单打勾）
- nginx.conf 改动核心
- init-admin.js 新流程
- 5 张管理表迁入
- staging 验证结果
- 已知 backlog（banners / announcements / /codes 页面 / admin_logs 双表）

---

## 完成判据（Sprint 2 整体）

全部 `[x]` 之后应该满足：

- [ ] `cd admin-frontend && npm run build` exit 0
- [ ] `cd admin-frontend && npm test` exit 0（至少 1 条用例）
- [ ] `cd admin-backend && npm test` exit 0（至少 2 条 smoke）
- [ ] `grep "admin123" admin-frontend/src/views/Login/index.vue` 无输出
- [ ] `grep "trust proxy" admin-backend/src/app.js` 有输出
- [ ] `grep "ALLOWED_SORTS\|sortField" admin-backend/src/controllers/novelController.js` 有输出
- [ ] `cat admin-backend/.gitignore-or-rule | grep .admin-credentials` 已忽略
- [ ] `nginx -t` 在测试服务器通过
- [ ] 5 张管理表（admin_users / admin_logs / banners / announcements / statistics_daily）由 migrate 流水线创建（迁移记录有 202605200001 这一行）
- [ ] `grep "continue-on-error" .github/workflows/ci.yml` 无输出
- [ ] staging 部署演练通过

完成后即可对外宣称"管理端 Sprint 2 完成，整站可发布给真实用户"。

---

## 不在本清单范围（提醒，避免误碰）

- 用户端 Sprint 1 任务（已在 [用户端清单](2026-05-20-用户端优化任务执行清单.md)）
- Express 4 → 5 主版本统一（治理项，跨子工程）
- bcryptjs / multer / helmet 主版本对齐（治理项）
- 整合 admin_logs / code_admin_logs 双表为统一视图（backlog）
- 实现 `/codes` 管理前端页面（如果产品需要 → 单独 Sprint）
- 实现 banners / announcements 控制器（如果产品需要 → 单独 Sprint）
- ECharts 按需引入（性能优化，不阻塞发布）
- TailwindCSS / Element Plus preflight 冲突排查（如有 UI 错位再做）
- admin Login 失败计数 + 锁定（增强项，下个 Sprint）
- admin-frontend 移动端适配（增强项）
- ESLint / Prettier 统一（治理项）

---

## 时间预估汇总

| 阶段 | 任务数 | 预估工时（单人） |
|---|---|---|
| 阶段 1 — 解除攻击链 + 修构建 | 7 | 0.5 天 |
| 阶段 2 — 默认密码 + JWT | 3 | 0.5 天 |
| 阶段 3 — 数据库与迁移 | 4 | 0.5 天 |
| 阶段 4 — 上传安全 + errorHandler | 2 | 0.5 天 |
| 阶段 5 — Nginx | 1 | 0.5 天 |
| 阶段 6 — 测试 + 缺失功能 | 5（其中 T-6.3 可选） | 1.5 天 |
| 阶段 7 — CI + 大文件治理（部分可选） | 3 | 0.5 天 |
| 阶段 8 — 收尾 | 4 | 0.5 天 |
| **合计（含可选）** | **29** | **5 个工作日** |
| **合计（不含 T-6.3 / T-7.2 / T-7.3 三项可选）** | **26** | **3 个工作日** |

**节奏建议**：
- **单人单周**：1.5 周（不含可选项 1 周完成）
- **双人并行**：阶段 1+2（一人，链路依赖）+ 阶段 3+4（另一人）→ 阶段 5（一人）+ 阶段 6（另一人测试侧）→ 阶段 7+8 → **3 个工作日**

完成本清单 + 用户端清单后，**项目即具备整站对外发布的能力**。
