---
日期: 2026-05-21
作者: 项目优化负责人
范围: 用户端 Sprint 1 上线后冒烟 + 回归测试
被测站点: https://xs.momofx.cn
被测版本: commit f31fc4f（用户端 Sprint 1 优化）
依赖部署日志: 2026-05-21 11:28:06 部署完成
执行模式: 1 名 AI 负责人 + 5 名 AI 子代理并行
---

# 用户端 Sprint 1 测试清单

## 0. 给负责人 AI 看的总览

### 0.1 本次测试目标

验证 commit `f31fc4f` 的 14 项硬改动在生产环境真正生效，且未引入回归。**不是全量回归，是 Sprint 1 改动 + 核心链路**。

### 0.2 范围

**在范围**：
- 用户前端 https://xs.momofx.cn 全部功能
- 用户后端 API（通过前端间接调）
- 个人中心 5 链路（编辑资料 / 头像 / 修改密码 / 阅读偏好 / 通知中心）
- 安全加固验证（DEV 日志脱敏 / api-docs 门控 / 登录限流 / 外链 noreferrer）

**不在范围**：
- 管理端 https://admin.xxx 任何功能（属于 Sprint 2 测试）
- 部署脚本、CI 流程
- 性能压测（不在本轮）

### 0.3 共享约定（负责人在派单前必须先确认）

| 项 | 值 / 待负责人填写 |
|---|---|
| 生产 URL | https://xs.momofx.cn |
| 默认时区 | Asia/Shanghai |
| 5 个独立测试账号 | `{{ACCT_A}}` / `{{ACCT_B}}` / `{{ACCT_C}}` / `{{ACCT_D}}` / `{{ACCT_E}}` |
| 5 个对应密码 | （每人单独一份，不要共用） |
| Bug 严重性定义 | P0 阻断核心 / P1 严重影响体验 / P2 一般 / P3 改进项 |
| 报告交付 deadline | 负责人指定（建议 3 小时） |
| 报告交付路径 | `docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理X.md` |

> ⚠️ **必须每个子代理一个独立账号**——子代理 B 会触发登录限流（10 次/15min）会让账号短暂被锁，不能影响其他流。

### 0.4 红线（任何子代理违反立即停止）

- ❌ 不要删数据 / 不要批量改其他用户的资源
- ❌ 不要尝试越权访问其他账号的 profile / 订单 / 私信
- ❌ 不要尝试 SQL 注入 / XSS 真实 payload —— 这是冒烟测试，不是渗透
- ❌ 不要在生产数据库直接跑 SQL
- ❌ 不要修改任何代码 —— 测试只读 + 通过 UI 操作

### 0.5 工作流

```
   ┌──────────────────┐
   │ 负责人 AI（你）  │
   │  - 准备账号       │
   │  - 派 5 份清单    │
   │  - 收 5 份报告    │
   │  - 合并 + 排优先级 │
   └────────┬─────────┘
            │ 并行派发
   ┌────────┼────────┬────────┬────────┐
   ▼        ▼        ▼        ▼        ▼
 子代理A  子代理B  子代理C  子代理D  子代理E
 个人中心 认证安全 阅读核心 浏览发现 兼容回归
```

每个子代理：
1. 接清单 → 跑全部检查项 → 每项标 ✅/❌/⏸
2. 遇 bug → 按 §7 模板记录
3. 完成后输出独立报告文件

负责人：所有 5 份报告到齐后，按 §8 模板合并产出"用户端 Sprint 1 测试总报告"。

---

## 1. 子代理 A — 个人中心 5 条链路

### 1.1 你的范围

验证 Sprint 1 阶段 2（个人中心 MVP 收口）落地是否真在生产能跑通。

**测试账号**：`{{ACCT_A}}` / `{{PASS_A}}`（独占）
**预计耗时**：30 分钟
**前置**：浏览器使用 Chromium 系（Chrome / Edge），打开 DevTools → Network + Console，开始测试前清空 Network 面板。

### 1.2 检查项（按顺序）

| # | 链路 | 操作步骤 | 预期 | 观察点 |
|---|---|---|---|---|
| A1 | 登录入口 | 1. 访问 https://xs.momofx.cn<br>2. 用 ACCT_A / PASS_A 登录 | 跳回首页或上一页，右上角出现头像 | Network: `POST /api/auth/login` 返回 200，response 含 `accessToken`+`refreshToken`<br>Console: 无 token 明文 |
| A2 | 编辑资料 | 1. 点头像→个人中心<br>2. 进 `/profile/edit`<br>3. 改昵称为 `测试_A_<时间戳>`<br>4. 点保存 | 接口 200，弹保存成功，刷新页昵称仍为新值 | Network: `PUT /api/users/profile` 200 |
| A3 | 头像上传 | 1. 仍在 `/profile/edit`<br>2. 点头像选一张 < 2MB 的本地图片（PNG/JPG）<br>3. 等上传完成 | 头像即时更新；强刷后仍是新头像 | Network: `POST /api/users/avatar` 200，返回 url 包含 `/uploads/avatars/`<br>**红线**：不要上传含真实人脸或敏感信息的图片，用纯色或公开素材 |
| A4 | 修改密码 | 1. 进 `/security`<br>2. 旧密码=PASS_A，新密码=PASS_A 后加 `_v2`<br>3. 提交 | 接口 200，提示需重新登录 | 改完后用 **新密码** 重新登录，能登进 → 测试结束后 **必须改回 PASS_A**，否则下次跑会卡 |
| A5 | 阅读偏好 | 1. 进 `/profile/preferences`<br>2. 拖字号到 18<br>3. 切夜间主题<br>4. 关页 → 重进 | 字号=18，主题=夜间 | Network: `PUT /api/preferences` 200 |
| A6 | 通知中心 | 1. 进 `/profile/notifications` | 至少看到 1 条种子欢迎通知 | 点任意一条 → Network 出现 `PATCH /api/notifications/:id/read` 200，红点消失 |

### 1.3 产出

- 6 项 ✅/❌/⏸ 一一标注
- 任一项失败按 §7 模板落 bug
- 若 A4 测完没改回原密码，**请在报告里显式说明**，避免下个测试卡死
- 报告路径：`docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理A.md`

---

## 2. 子代理 B — 认证 & 安全加固验证

### 2.1 你的范围

验证 Sprint 1 阶段 1（恢复闸门）+ 阶段 3（安全加固）相关条目在生产是否真生效。**这条流会触发登录限流，独立账号是硬要求**。

**测试账号**：`{{ACCT_B}}` / `{{PASS_B}}`（独占，会被锁 15 分钟）
**预计耗时**：45 分钟
**前置**：DevTools 打开 Network + Console，准备一个无痕窗口。

### 2.2 检查项

| # | 项 | 对应清单任务 | 操作 | 预期 |
|---|---|---|---|---|
| B1 | DEV 日志脱敏 | T-1.4 | 1. 登录 ACCT_B<br>2. 任意翻页触发 API<br>3. 看 Console | Console 出现的 `[api] →` 行**只有** method+url+(authed\|anon)；**不应**看到 `tokenPreview`、`Authorization: Bearer xxx...` 之类的 token 片段 |
| B2 | api-docs 生产门控 | T-1.6 | 无痕访问 https://xs.momofx.cn/api-docs 与 `/api-docs.json` | 两条都返回 404 / 不可达；**绝对不应**渲染 Swagger UI |
| B3 | 登录限流 | T-3.3 | 1. 登出<br>2. 故意用错密码连续点登录 11 次（间隔 < 1 秒） | 第 11 次（或更早）开始返回 429 Too Many Requests；response header 含 `RateLimit-*` 或类似<br>**红线**：用 ACCT_B 自己的用户名+错密码，**不要**用别人的用户名 |
| B4 | 登录限流恢复 | T-3.3 | B3 触发后，立即用对的密码登 | 仍然 429，等 15 分钟后再试一次 → 应能成功登入<br>（如果 deadline 紧，标 ⏸ 跳过等待，注明"已 429 触发但未等待恢复"） |
| B5 | JWT algorithm | T-3.4（黑盒） | 1. 用 B 登录后，DevTools → Application → LocalStorage 拿 accessToken<br>2. 把 token 第一段（header）base64 解码 | 解出 `{"alg":"HS256","typ":"JWT"}`；**不应**是 `none` 或其他算法 |
| B6 | 外链 noreferrer | T-3.1 | 1. 进 `/profile/notifications`<br>2. 任意一条带跳转的通知，右键检查 `<a>` 或 `window.open` 触发 | 跳转新窗口，DevTools Network 上的 Referer 字段为空或被剥离<br>（若没有可跳的通知，标 ⏸ 注明"种子通知无外链"） |
| B7 | /api/health 不限流 | T-1.5 | 无痕浏览器开 DevTools，连续刷 `https://xs.momofx.cn/api/health` 30 次 | 全部 200，不出现 429 |

### 2.3 产出

- 7 项 ✅/❌/⏸
- B3/B4 触发的 429 截图存档
- B5 解码出来的 header JSON 截图（**只发 header，不发 payload，不发 signature**）
- 报告路径：`docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理B.md`

---

## 3. 子代理 C — 阅读核心回归

### 3.1 你的范围

确认 Sprint 1 没有把阅读链路碰坏，并验证阅读偏好与个人中心 §A5 的字号/主题联动。**T-4.1（ReadingPage 1030 行）已被 Sprint 推迟，但本轮要确保在改之前阅读页是绿的，作为下个 Sprint 的回归基准**。

**测试账号**：`{{ACCT_C}}` / `{{PASS_C}}`（独占）
**预计耗时**：45 分钟
**前置**：登录后建议先去 `/profile/preferences` 把字号/主题恢复默认（避免被前一次测试影响）。

### 3.2 检查项

| # | 项 | 操作 | 预期 |
|---|---|---|---|
| C1 | 进入阅读 | 首页或书架找一本可读小说 → 点章节进入阅读页 | 章节正常加载，标题与正文对应 |
| C2 | 翻页（按钮） | 鼠标点下一页 / 上一页按钮 | 章节切换流畅，无白屏；URL 章节号变更 |
| C3 | 翻页（键盘） | ← / → 或 PgUp/PgDn | 同 C2 |
| C4 | 翻页（滚轮） | 滚到底部触底 | 自动加载下一章 **或** 出"下一章"提示按钮（取决于实现） |
| C5 | 字号联动 | 阅读页内调字号（如有侧栏/工具栏）→ 离开阅读页→进 `/profile/preferences` | 偏好里字号是刚才设的值；反过来在偏好里改后回阅读页也应同步 |
| C6 | 夜间主题联动 | 在 `/profile/preferences` 切夜间 → 回阅读页 | 阅读页背景/字色立即夜间化，无需刷新 |
| C7 | 护眼模式 | 阅读页打开护眼模式（EnhancedEyeCareMode） | 颜色变绿调，关闭后恢复 |
| C8 | TTS 朗读 | 点朗读按钮（如功能开放） | 浏览器 SpeechSynthesis 正常发声；停止按钮可断<br>（不可用就标 ⏸，注明"环境无 TTS 引擎"） |
| C9 | 章节目录 | 点目录 → 跳到指定章节 | 跳转准确，进度条/章节号同步 |
| C10 | 离开 + 回来 | 阅读到第 N 章 → 退到首页 → 重进同本书 | 自动定位到第 N 章 / 上次进度 |

### 3.3 产出

- 10 项 ✅/❌/⏸
- 失败项 §7 模板落 bug，标注章节号 + 浏览器
- 报告路径：`docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理C.md`

---

## 4. 子代理 D — 浏览 / 发现 / 搜索

### 4.1 你的范围

非阅读非个人中心的对外门面：首页、书库、分类、搜索、详情页、SEO 基本检查。**Sprint 1 没动这块，所以这是反向回归**——确保没意外打挂。

**测试账号**：`{{ACCT_D}}` / `{{PASS_D}}`（独占）
**预计耗时**：30 分钟

### 4.2 检查项

| # | 项 | 操作 | 预期 |
|---|---|---|---|
| D1 | 首页加载 | 无痕访问 https://xs.momofx.cn | 200，首屏 < 3 秒，无控制台红错 |
| D2 | 封面图 | 首页 / 书库内任意 5 本封面 | 全部加载（用英文 kebab-case 路径，commit 7be56c5 之后），无 404 |
| D3 | 分类筛选 | 点分类 / 标签 / 排序 | 列表正确切换，URL query 同步 |
| D4 | 搜索 | 顶部搜索框输关键词 → 提交 | 跳搜索结果页，结果非空时高亮匹配词 |
| D5 | 搜索空态 | 搜一个明显没有的词如 `zzzqqq无此书9999` | 出"没有结果"友好态，不白屏不报错 |
| D6 | 详情页 | 任意小说详情页 | 简介、章节列表、作者、标签都能看到 |
| D7 | 加书架 | 详情页点"加入书架"（已登录） | 按钮变状态，再进书架能看到 |
| D8 | 移除书架 | 书架内移除一本 | 列表立即更新，刷新仍移除 |
| D9 | 路由 404 | 输入 https://xs.momofx.cn/this-page-does-not-exist | 渲染 404 页，**不应**白屏或回环跳转 |
| D10 | SEO 头 | 首页 + 1 个详情页 → DevTools Elements 看 `<head>` | `<title>`、`<meta name="description">` 非空且与页面相关 |
| D11 | robots/sitemap | 直接访问 `/robots.txt` 与 `/sitemap.xml` | 至少 robots.txt 200（如未配置就标 ⏸ 注明） |

### 4.3 产出

- 11 项 ✅/❌/⏸
- D2 失败的封面记下英文文件名给负责人 → 反查路径迁移
- 报告路径：`docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理D.md`

---

## 5. 子代理 E — 跨端兼容 + 控制台 + 错误路径

### 5.1 你的范围

兜底用：跨浏览器、跨视口、控制台健康度、网络异常时表现。**这条流不深入业务，重点在"看像不像出问题"**。

**测试账号**：`{{ACCT_E}}` / `{{PASS_E}}`（独占）
**预计耗时**：40 分钟
**前置**：至少 2 个浏览器（Chromium 系 + Firefox 系），1 台移动设备或 DevTools 设备模拟。

### 5.2 检查项

| # | 项 | 操作 | 预期 |
|---|---|---|---|
| E1 | 跨浏览器 — Chrome | Chrome 登录 → 浏览首页 → 进阅读页 → 退出 | 全程无报错，UI 元素位置正常 |
| E2 | 跨浏览器 — Firefox | 同上 | 同 E1 |
| E3 | 跨浏览器 — Safari（如有） | 同上 | 同 E1（无 Safari 标 ⏸） |
| E4 | 移动视口 | DevTools 切 iPhone 12 / Pixel 5 → 走 E1 流程 | 顶栏汉堡菜单可用，无横向滚动条 |
| E5 | 平板视口 | DevTools 切 iPad | 同 E4 |
| E6 | 控制台健康 | 跑完 E1 后查 Console | **0 个 red error**；warning 可有；如出现 red error 全部截图给负责人 |
| E7 | 慢网络 | DevTools Network → Slow 3G → 刷首页 | 首屏 loading 态出现，最终渲染完成；**不应**长时间空白 |
| E8 | 离线 | DevTools Network → Offline → 刷首页 | 出友好"网络断开"提示或 SW 缓存可用；**不应**白屏 |
| E9 | 401 处理 | 登录后手动改 LocalStorage 里 accessToken 第一段 → 触发任意需鉴权请求 | 自动跳登录页或弹"请重新登录"；**不应**死循环重试 |
| E10 | 表单防抖 | 任意提交表单（如登录），疯狂连点 5 次 submit | 至多触发 1-2 次请求；**不应**触发 5 次 |
| E11 | XSS 涂鸦（无害） | 在昵称里输 `<b>hello</b>`（**纯 HTML 标签，不带 script**）→ 保存 | 渲染时显示字面量 `<b>hello</b>` 或被转义；**不应**真的加粗（说明做了转义） |

> ⚠ E11 红线：**只用 `<b>hello</b>` 这种无害标签**，绝对不要用 `<script>`、`<img onerror>` 等真实 payload。我们是验证转义而不是渗透。

### 5.3 产出

- 11 项 ✅/❌/⏸
- E6 控制台 red error 全部贴（含浏览器版本）
- E11 截图保存（昵称测完后**改回原值**）
- 报告路径：`docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理E.md`

---

## 6. 公共冒烟（每个子代理开始前 2 分钟先跑一遍）

如果 §6 任一项就挂了，**直接挂起所有测试**通知负责人，避免在已挂的环境上浪费时间。

| # | 项 | 操作 | 预期 |
|---|---|---|---|
| S1 | 站点可达 | 无痕访问 https://xs.momofx.cn | 200，HTML 正常返回 |
| S2 | API 可达 | 访问 https://xs.momofx.cn/api/health | 200，JSON 含 status 字段 |
| S3 | 静态资源 | DevTools Network 看首页 .js / .css 资源 | 全部 200，无 mixed content |
| S4 | TLS | 浏览器地址栏锁 | 绿锁，证书有效 |

挂起信号格式（写在报告最顶上）：

```
🚨 公共冒烟挂起
S?: <症状>
我已停止后续测试，等待负责人指示。
```

---

## 7. Bug 报告模板（子代理用）

**每发现一个 bug 就在自己报告里加一段，按这个格式**：

```markdown
### Bug #<子代理>-<编号>  [P0|P1|P2|P3]

**标题**: <一句话描述>

**对应检查项**: §<n>.<m> <项名>

**重现步骤**:
1. ...
2. ...
3. ...

**预期**: <你预期发生什么>
**实际**: <实际发生了什么>

**环境**:
- 账号: ACCT_X（不要写密码！）
- 浏览器: Chrome 121 / Firefox 122 / ...
- 设备: 桌面 1920x1080 / iPhone 12 模拟
- 时间: 2026-05-21 HH:MM

**证据**:
- Console 截图 / 报错堆栈（关键行贴上）
- Network 失败请求的 Status + Response（**屏蔽掉 token 与个人信息**）
- 截图文件名: `bug-<子代理>-<编号>.png`

**初步判断**（可选）: <你猜可能是哪一层的问题>
```

### 7.1 严重性定义（再贴一遍）

| 级别 | 定义 | 示例 |
|---|---|---|
| P0 | 阻断核心链路，必须立即停发 | 登录全挂、阅读页打不开、付费链路失败 |
| P1 | 严重影响体验，但有 workaround | 头像上传偶发失败、夜间主题不联动 |
| P2 | 一般功能问题 | 翻页动画卡顿、空态文案错别字 |
| P3 | 改进 / 体验细节 | 按钮 hover 阴影、移动端 padding 偏小 |

---

## 8. 负责人合并报告模板

5 份子报告到齐后，由负责人 AI 在 `docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试总报告.md` 写：

```markdown
---
日期: 2026-05-21
作者: 项目优化负责人
基于: 5 份子代理报告（A-E）
被测版本: commit f31fc4f
---

# 用户端 Sprint 1 测试总报告

## 0. 一句话结论

<通过 / 有条件通过 / 不通过>。共发现 X 个 bug（P0=a, P1=b, P2=c, P3=d）。<是否阻断对外发布>。

## 1. 子代理执行情况

| 子代理 | 范围 | 完成项/总项 | 状态 |
|---|---|---|---|
| A | 个人中心 5 链路 | x/6 | ✅/⚠️/❌ |
| B | 认证 & 安全加固 | x/7 | ✅/⚠️/❌ |
| C | 阅读核心回归 | x/10 | ✅/⚠️/❌ |
| D | 浏览/发现/搜索 | x/11 | ✅/⚠️/❌ |
| E | 跨端 + 控制台 + 错误路径 | x/11 | ✅/⚠️/❌ |

## 2. Sprint 1 关键改动验证落地表

| 任务 | 子代理检查项 | 结果 |
|---|---|---|
| T-1.4 DEV 日志脱敏 | B1 | ✅/❌ |
| T-1.6 api-docs 门控 | B2 | ✅/❌ |
| T-3.1 noreferrer | B6 | ✅/❌ |
| T-3.3 登录限流 | B3/B4 | ✅/❌ |
| T-3.4 JWT HS256 | B5 | ✅/❌ |
| T-2.x 个人中心 5 链路 | A1-A6 | ✅/❌ |
| 阅读核心未被打挂 | C1-C10 | ✅/❌ |
| 跨端兼容 | E1-E5 | ✅/❌ |

## 3. Bug 总清单（按优先级排序）

### P0
- Bug A-3: ...（链接到子报告）

### P1
- Bug B-2: ...
- Bug C-5: ...

### P2 / P3
- ...

## 4. 发布建议

- [ ] 全部 P0 修完才能继续发布
- [ ] P1 列入下一周期
- [ ] P2/P3 入 backlog（链接到 Sprint 2 清单）

## 5. 遗留与跟进

- 哪些子代理挂起未跑完
- 测试期间发现的清单外问题
- 推荐下一 Sprint 优先收的方向
```

---

## 9. 派单脚本（负责人 AI 直接复制改名用）

派给子代理 X 的提示词模板：

```
你是用户端 Sprint 1 测试的子代理 X，范围见 docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试清单.md §X。

测试账号: ACCT_X = ...
密码: PASS_X = ...
被测站点: https://xs.momofx.cn
报告交付路径: docs/AI完成后报告文档/2026-05-21-用户端Sprint1测试报告-子代理X.md
deadline: <时间>

执行规则:
1. 先跑 §6 公共冒烟，挂了立即停并报告
2. 按 §X.2 表格逐项执行，每项标 ✅/❌/⏸
3. bug 按 §7 模板写入你的报告
4. 严守 §0.4 红线（不删数据、不越权、不真注入、不动代码、不动生产 DB）
5. 完成后只输出报告文件，不修改任何代码

开始。
```

---

## 10. 检查清单结尾自检（负责人发派单前过一遍）

- [ ] 5 个测试账号已就位且互不重叠
- [ ] 5 个账号 §A4 改密码场景已与子代理 A 单独沟通"测完务必改回原密码"
- [ ] 全员 deadline 一致
- [ ] §0.4 红线已读给每个子代理
- [ ] 子代理 B 的限流测试与其他子代理时间错开（避免 IP 限流误伤）
- [ ] 子代理 E 的 E11 涂鸦已和 D 协调"测完改回原昵称"

> 全部勾完，再派单。

