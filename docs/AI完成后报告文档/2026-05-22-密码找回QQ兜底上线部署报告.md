文档日期：2026-05-22

# 密码找回 QQ 兜底上线部署报告

## 上线内容

- 用户端忘记密码 / 重置密码临时兜底提示改为管理员 QQ：`472990945`。
- 用户端忘记密码 / 重置密码页面按钮改为复制管理员 QQ。
- 用户后端 `/api/auth/forgot-password` 与 `/api/auth/reset-password` 返回带管理员 QQ 的兜底提示。
- 新增迁移 `202605220001__set_default_contact_qq.sql`，将 `system_contact.id=1` 的默认客服 QQ 设置为 `472990945`。

## 部署范围

- 用户前端：`ai-xsread-vue3/dist`
- 用户后端：`backend/src/controllers/authController.js`
- 数据库迁移：`backend/database/migrations/202605220001__set_default_contact_qq.sql`

## 部署前备份

- 数据库备份：`/www/wwwroot/xs.momofx.cn/backups/ai_xsread_before_20260522_155204.sql`
- 部署前 commit 记录：`/www/wwwroot/xs.momofx.cn/backups/before-deploy-commit.txt`
- `.env` 备份：`/www/wwwroot/xs.momofx.cn/backups/backend.env.backup.20260522_155204`

## 验证结果

- 本地 `node -c backend/src/app.js`：通过。
- 本地 `cd ai-xsread-vue3 && npm run build`：通过。
- 本地 `cd admin-frontend && npm run build`：通过。
- `python backend/scripts/deploy.py --frontend --backend --dry-run`：通过，确认仅部署用户前端 dist 与用户后端相关文件。
- `python backend/scripts/deploy.py --frontend --backend`：通过。
- 服务器 `pm2 status`：`xsread-backend` online。
- 服务器 `/api/health`：200，`latestVersion=202605220001`，`total=26`。
- 服务器 `/api/system/contact`：200，返回 `qq=472990945`。
- 服务器 `/api/auth/forgot-password`：200，返回“密码找回暂未开放，请联系管理员 QQ：472990945 处理。”
- 线上 `/forgot-password`：HTTP 200。

## 已知问题

- `pm2 logs xsread-backend` 中存在部署前已出现的每小时任务报错：`PROCEDURE ai_xsread.sp_batch_update_views does not exist`。本次发布未涉及该存储过程。
- 构建输出仍提示 `baseline-browser-mapping` / `Browserslist caniuse-lite` 数据偏旧，这是现有依赖数据提示，本次未处理。
- `npm ci --omit=dev` 输出若干依赖 deprecation warning，本次未升级依赖。

## 回滚预案

1. 停止服务：`pm2 stop xsread-backend xsread-admin-backend`
2. 还原数据库：`mysql -u <user> -p ai_xsread < /www/wwwroot/xs.momofx.cn/backups/ai_xsread_before_20260522_155204.sql`
3. 按 `/www/wwwroot/xs.momofx.cn/backups/before-deploy-commit.txt` 记录回滚代码。
4. 重新安装依赖并启动：`cd /www/wwwroot/xs.momofx.cn/backend && npm ci --omit=dev && pm2 start /www/wwwroot/xs.momofx.cn/ecosystem.config.js --only xsread-backend --update-env`
5. 重新验证 `/api/health` 与用户首页。
