# 2026-05-27 首页阅读体验与数据工具上线部署报告

文档日期：2026-05-27

## 一、上线范围

- 部署 commit：`cfb6db4`（`优化: 完善阅读体验与小说数据工具`）
- commit 范围：`820a444..cfb6db4`
- 部署目标：`xs.momofx.cn`
- 部署方式：`backend/scripts/deploy.py --all-with-admin`
- 部署内容：用户前端、用户后端、管理前端、管理后端、封面图片资源

## 二、本次主要改动

1. 首页书城交互优化
   - 将“推荐榜”提升为首页顶部第一个 tab，顺序为“推荐榜 → 精选 → 都市言情...”。
   - 移除未实现的“完本榜 / 口碑榜 / 飙升榜 / 完整榜单”文案。
   - 支持移动端在顶部标题栏和内容区左右滑动切换分类。
   - 推荐榜与精选榜做展示差异化：精选优先避开推荐榜首屏已出现书籍。
   - 继续阅读提示改为底部导航上方的极简悬浮胶囊，并保留 15 秒自动消失。

2. 阅读与个人中心优化
   - 新增小说详情页内联阅读体验组件。
   - 补充详情页内联阅读相关测试。
   - 优化个人中心阅读统计展示、周阅读柱状图和账号邮箱展示守护。
   - 用户统计接口返回分钟级阅读时长，并保留秒级明细。

3. 管理端与数据工具
   - 完善管理端小说统计编辑逻辑与测试。
   - 新增小说初始统计回填脚本。
   - 新增批量上传剩余小说脚本和微信上传进度监控脚本。
   - 新增用户统计控制器测试。

4. 内容资产与文档
   - 新增/更新一批 AI 封面图片资源。
   - 补充三本小说本地到线上同步上传报告。
   - 补充小说初始统计数据补齐上线报告。

## 三、部署前检查

1. Git 提交与推送
   - 已提交：`cfb6db4`
   - 已推送：`origin/main`
   - 未纳入提交的本地文件：`backend/.env`、`backend/scripts/__pycache__/watch-upload-progress-weixin.cpython-312.pyc`
   - 原因：`.env` 为本地环境配置，pyc 为缓存产物，不应进入仓库。

2. 数据库备份
   - 已在部署前执行线上数据库备份。
   - 备份文件：`/www/backup/ai-xsread/predeploy-20260527-124105-ai_xsread.sql`
   - 备份方式：`mysqldump --single-transaction`

3. 本地验证
   - `node -c backend/src/app.js`：通过
   - `ai-xsread-vue3 npm test`：28 个测试文件通过，122 条测试通过
   - `backend npm test`：47 个测试套件通过，269 条测试通过，2 条跳过
   - `admin-backend npm test`：8 个测试套件通过，26 条测试通过
   - `ai-xsread-vue3 npm run build`：通过
   - `admin-frontend npm run build`：通过

## 四、部署过程

1. dry-run
   - 命令：`python backend/scripts/deploy.py --all-with-admin --dry-run`
   - 结果：通过
   - 确认点：目标路径、rsync `--checksum`、前后端目录、PM2 进程名均符合预期。

2. 正式部署
   - 命令：`python backend/scripts/deploy.py --all-with-admin`
   - 图片资源：上传 94 个封面资源文件。
   - 用户前端：构建成功，上传 52 个新增/变更文件，删除 49 个旧 hash 资源文件。
   - 用户后端：上传 5 个新增/变更文件，PM2 reload `xsread-backend` 成功。
   - 管理前端：构建成功，远端文件无新增变化。
   - 管理后端：PM2 reload `xsread-admin-backend` 成功。

## 五、数据库迁移版本

- 本次无新增迁移文件。
- 线上迁移摘要：累计 28 个迁移。
- 最新迁移版本：`202605220003`
- 最新迁移说明：`add novel hot score`

## 六、部署后验证

1. PM2 状态
   - `xsread-backend`：`online`
   - `xsread-admin-backend`：`online`

2. 健康检查
   - `https://xs.momofx.cn/api/health`：HTTP 200
   - 返回摘要：`status=healthy`，`db=up`，`latestVersion=202605220003`
   - `https://xs.momofx.cn/`：HTTP 200
   - 管理后端本机健康检查：HTTP 200，返回 `{"status":"ok"}`

3. 日志检查
   - 部署后日志包含：
     - `✅ 数据库迁移完成，最新版本: 202605220003`
     - `⏰ 定时任务已启动`
     - `🚀 MOMO小说后端服务启动成功`
   - 发现的历史噪音：
     - 03:00 数据库备份脚本路径历史错误。
     - 04:00 `uploads/temp` 不存在导致清理临时文件失败。
   - 部署后新日志：
     - MySQL2 `acquireTimeout/timeout` 配置 warning，属既有配置警告。
     - PM2 reload 触发旧进程 `SIGINT`，属 reload 正常行为。
   - 未发现部署后新增 500 崩溃或服务不可用现象。

## 七、回滚预案

1. 代码回滚
   - 回滚目标：`820a444`
   - 操作前必须停止相关服务或确认只回滚静态资源。
   - 推荐步骤：
     - `pm2 stop xsread-backend xsread-admin-backend`
     - `git reset --hard 820a444`
     - 重新执行 `npm ci --omit=dev`
     - 通过 `backend/scripts/deploy.py` 重新部署对应版本
     - `pm2 restart xsread-backend xsread-admin-backend --update-env`

2. 数据库回滚
   - 本次无新增迁移，正常情况下不需要数据库回滚。
   - 如需恢复部署前数据库状态，使用备份：
     - `/www/backup/ai-xsread/predeploy-20260527-124105-ai_xsread.sql`
   - 数据库恢复前必须先停服务，避免写入丢失。

3. 静态资源回滚
   - 用户前端如需快速回退，可重新构建并部署 `820a444` 对应 dist。
   - 封面资源为新增/更新资源；如需回退单个封面，应根据 Git 历史恢复对应文件后重新部署图片资源。

## 八、已知问题与后续建议

1. 线上定时备份脚本路径存在历史错误：`bash ./backend/scripts/backup.sh` 找不到文件。建议单独修复定时任务工作目录或脚本路径。
2. `uploads/temp` 不存在导致每日清理任务报错。建议启动时自动创建目录，或清理任务对不存在目录静默跳过。
3. MySQL2 配置项 `acquireTimeout/timeout` 已产生 warning。建议后续统一清理为 MySQL2 支持的配置项。
4. 管理前端存在大 chunk warning（Element Plus / ECharts）。本次不影响上线，但建议后续做路由级拆包或 manualChunks 优化。
