# 文字之境 UI 组件使用指南

> **文档版本**: 1.0.0  
> **更新日期**: 2025年10月28日  
> **适用于**: ai-xsread-vue3 前端项目

---

## 📚 目录

1. [基础组件](#基础组件)
2. [反馈组件](#反馈组件)
3. [导航组件](#导航组件)
4. [阅读组件](#阅读组件)
5. [搜索组件](#搜索组件)
6. [工具函数](#工具函数)

---

## 基础组件

### LazyImage - 图片懒加载

**位置**: `src/components/common/LazyImage.vue`

**基础用法**:
```vue
<LazyImage
  :src="novel.cover"
  alt="小说封面"
  placeholder="/placeholder.jpg"
  error-image="/error.jpg"
/>
```

**Props**:
- `src` (String, required) - 图片地址
- `alt` (String) - 替代文本
- `placeholder` (String) - 占位图
- `errorImage` (String) - 错误图
- `lazy` (Boolean, default: true) - 是否懒加载
- `rootMargin` (String, default: '50px') - 提前加载距离

**Events**:
- `@load` - 加载成功
- `@error` - 加载失败

---

### SkeletonScreen - 骨架屏

**位置**: `src/components/common/SkeletonScreen.vue`

**基础用法**:
```vue
<!-- 小说卡片骨架 -->
<SkeletonScreen type="novel-card" :count="6" />

<!-- 章节列表骨架 -->
<SkeletonScreen type="chapter-list" :count="10" />

<!-- 阅读页骨架 -->
<SkeletonScreen type="reading" />
```

**Props**:
- `type` (String) - 骨架类型
  - `novel-card` - 小说卡片
  - `novel-detail` - 小说详情页
  - `chapter-list` - 章节列表
  - `reading` - 阅读页
  - `list` - 通用列表
- `count` (Number, default: 3) - 重复数量

---

### EmptyState - 空状态

**位置**: `src/components/common/EmptyState.vue`

**基础用法**:
```vue
<!-- 使用预设 -->
<EmptyState
  type="bookshelf-empty"
  @action="goToHome"
/>

<!-- 自定义 -->
<EmptyState
  title="没有找到结果"
  message="试试其他关键词吧"
  actionText="返回首页"
  @action="handleAction"
/>
```

**预设类型**:
- `bookshelf-empty` - 书架为空
- `search-no-results` - 搜索无结果
- `comments-empty` - 评论为空
- `network-error` - 网络错误
- `no-data` - 暂无数据
- `reading-history-empty` - 无阅读记录
- `favorites-empty` - 无收藏
- `404` - 页面不存在

---

### Loading - 加载动画

**位置**: `src/components/common/Loading.vue`

**基础用法**:
```vue
<!-- 页面内加载 -->
<Loading
  variant="circle"
  text="加载中..."
  size="medium"
/>

<!-- 全屏加载 -->
<Loading
  variant="book"
  :fullscreen="true"
  :overlay="true"
  :smart-text="true"
/>
```

**Props**:
- `variant` (String) - 动画类型
  - `circle` - 圆圈旋转
  - `dots` - 点跳动
  - `wave` - 波浪
  - `book` - 书本翻页
  - `gradient` - 渐变圆环
- `text` (String) - 提示文本
- `size` (String) - 尺寸 (small/medium/large)
- `fullscreen` (Boolean) - 全屏模式
- `overlay` (Boolean) - 叠加层
- `smartText` (Boolean) - 智能提示

---

## 反馈组件

### Toast - 消息提示

**位置**: `src/components/common/Toast.vue`, `src/utils/toast.js`

**基础用法**:
```javascript
import toast from '@/utils/toast'

// 成功提示
toast.success('addToBookshelf')  // 使用预设
toast.success('操作成功！')      // 自定义

// 错误提示
toast.error('network')
toast.error('操作失败，请重试')

// 警告提示
toast.warning('unsaved')

// 信息提示
toast.info('loading')

// 完整配置
toast.show({
  type: 'success',
  title: '提示',
  message: '操作成功',
  duration: 3000,
  position: 'top-center',
  closable: true,
  onClose: () => console.log('关闭')
})
```

**预设消息**:

成功类:
- `addToBookshelf` - 已加入书架，开始温柔的阅读旅程吧 ✨
- `like` - 感谢你的喜欢 ❤️
- `comment` - 评论发表成功，期待更多交流 💬
- `collect` - 收藏成功，可以在书架找到它哦 📚

错误类:
- `network` - 网络似乎开了小差，请稍后再试 😊
- `notFound` - 抱歉，这本书找不到了 😢
- `forbidden` - 需要登录才能继续哦 🔐

---

### Notification - 通知系统

**位置**: `src/components/common/Notification.vue`

**使用方式**:
```vue
<template>
  <Notification ref="notificationRef" />
</template>

<script setup>
import { ref } from 'vue'
import Notification from '@/components/common/Notification.vue'

const notificationRef = ref(null)

function showNotification() {
  notificationRef.value.addNotification({
    type: 'success',
    title: '🎉 成就解锁',
    message: '恭喜获得「初出茅庐」成就！',
    description: '阅读第一本小说',
    duration: 5000,
    actions: [
      { text: '查看', onClick: () => console.log('查看成就') },
      { text: '知道了', onClick: () => {} }
    ]
  })
}
</script>
```

---

### LikeButton - 点赞按钮

**位置**: `src/components/common/LikeButton.vue`

**基础用法**:
```vue
<LikeButton
  :liked="novel.isLiked"
  :count="novel.likes"
  size="medium"
  @like="handleLike"
  @unlike="handleUnlike"
/>
```

**Props**:
- `liked` (Boolean) - 是否已点赞
- `count` (Number) - 点赞数
- `size` (String) - 尺寸 (small/medium/large)

**Events**:
- `@like` - 点赞
- `@unlike` - 取消点赞

**动画效果**:
- 心跳缩放
- 粒子爆炸
- 数字弹跳
- 移动端震动

---

## 导航组件

### Breadcrumb - 面包屑导航

**位置**: `src/components/common/Breadcrumb.vue`

**基础用法**:
```vue
<Breadcrumb />
```

**特性**:
- 自动根据路由生成
- 首页图标支持
- 当前页面高亮
- ARIA 标签完整

---

### BackToTop - 返回顶部

**位置**: `src/components/common/BackToTop.vue`

**基础用法**:
```vue
<BackToTop />
```

**特性**:
- 滚动超过 300px 自动显示
- 进度环显示滚动百分比
- 平滑滚动动画
- 悬停变色效果
- 移动端震动反馈

---

### KeyboardHelp - 键盘帮助

**位置**: `src/components/common/KeyboardHelp.vue`

**基础用法**:
```vue
<KeyboardHelp :show="showHelp" @update:show="showHelp = $event" />
```

**Composable 用法**:
```javascript
import { useKeyboard } from '@/composables/useKeyboard'

const { showKeyboardHelp } = useKeyboard()
```

**快捷键列表**:
- 全局: `?` `/` `Esc` `H` `B` `R` `P`
- 阅读: `← →` `Space` `F` `S` `M` `D`

---

## 阅读组件

### ReadingProgress - 阅读进度

**位置**: `src/components/reading/ReadingProgress.vue`

**基础用法**:
```vue
<ReadingProgress
  :current-chapter="10"
  :total-chapters="100"
  :session-read-time="1800"
  :today-read-time="3600"
/>
```

**Props**:
- `currentChapter` (Number) - 当前章节
- `totalChapters` (Number) - 总章节数
- `sessionReadTime` (Number) - 本次阅读时长（秒）
- `todayReadTime` (Number) - 今日阅读时长（秒）

**显示内容**:
- 顶部进度条（带光晕）
- 圆环进度指示器
- 章节信息
- 阅读时长统计

---

### EyeCareMode - 护眼模式

**位置**: `src/components/reading/EyeCareMode.vue`

**基础用法**:
```vue
<EyeCareMode
  :settings="eyeCareSettings"
  @update:settings="updateSettings"
/>
```

**配置项**:
```javascript
{
  eyeCareMode: false,          // 护眼模式
  blueFilterIntensity: 30,     // 蓝光过滤 0-100
  restReminder: 0,             // 休息提醒 0/20/30/45/60分钟
  autoNightMode: false,        // 自动夜间模式
  nightModeStart: '22:00',     // 开始时间
  nightModeEnd: '06:00',       // 结束时间
  brightness: 100              // 亮度 50-100
}
```

---

### ReadingPresets - 阅读预设

**位置**: `src/components/reading/ReadingPresets.vue`

**基础用法**:
```vue
<ReadingPresets
  @preset-change="handlePresetChange"
  @show-custom="showCustomSettings"
/>
```

**预设模式**:
- `day` - 日间模式（明亮清晰）
- `night` - 夜间模式（柔和护眼）
- `vintage` - 怀旧模式（纸质书感）
- `eyecare` - 护眼模式（绿色舒适）
- `custom` - 自定义设置

---

## 搜索组件

### SearchSuggestions - 搜索建议

**位置**: `src/components/search/SearchSuggestions.vue`

**基础用法**:
```vue
<SearchSuggestions
  :keyword="searchKeyword"
  :show="showSuggestions"
  :hot-searches="hotSearches"
  :search-history="searchHistory"
  :suggestions="suggestions"
  :novel-previews="novelPreviews"
  @select="handleSelect"
  @select-novel="goToNovel"
  @clear-history="clearSearchHistory"
  @delete-history="deleteHistoryItem"
/>
```

**功能模块**:
- 热门搜索标签
- 搜索历史（可删除/清空）
- 实时搜索建议（关键词高亮）
- 小说预览卡片

---

### FilterPanel - 筛选面板

**位置**: `src/components/novel/FilterPanel.vue`

**基础用法**:
```vue
<FilterPanel
  :filters="currentFilters"
  :categories="categories"
  @update:filters="updateFilters"
  @apply="applyFilters"
  @reset="resetFilters"
/>
```

**筛选维度**:
- 分类（单选）
- 状态（多选：完结/连载/免费）
- 字数（滑块）
- 排序（下拉选择）

---

## 工具函数

### toast - 消息提示

**位置**: `src/utils/toast.js`

```javascript
import toast from '@/utils/toast'

// 快捷方法
toast.success('addToBookshelf')
toast.error('network')
toast.warning('unsaved')
toast.info('loading')

// 完整配置
toast.show({
  type: 'success',
  message: '操作成功',
  duration: 3000,
  position: 'top-center',
  closable: true
})

// 清除所有
toast.clearAll()
```

---

### useKeyboard - 键盘快捷键

**位置**: `src/composables/useKeyboard.js`

```javascript
import { useKeyboard } from '@/composables/useKeyboard'

// 全局快捷键
const { showKeyboardHelp } = useKeyboard()
```

**阅读页快捷键**:
```javascript
import { useReadingKeyboard } from '@/composables/useKeyboard'

useReadingKeyboard({
  onPrevChapter: () => console.log('上一章'),
  onNextChapter: () => console.log('下一章'),
  onToggleFullscreen: () => console.log('全屏'),
  onToggleSettings: () => console.log('设置'),
  onToggleMenu: () => console.log('目录'),
  onToggleTheme: () => console.log('主题')
})
```

---

## 完整示例

### 小说列表页

```vue
<template>
  <div class="novel-list-page">
    <!-- 面包屑 -->
    <Breadcrumb />
    
    <!-- 筛选面板 -->
    <FilterPanel
      :filters="filters"
      :categories="categories"
      @apply="handleFilter"
    />
    
    <!-- 加载中 -->
    <SkeletonScreen
      v-if="loading"
      type="novel-card"
      :count="12"
    />
    
    <!-- 小说列表 -->
    <div v-else-if="novels.length" class="novel-grid">
      <NovelCardEnhanced
        v-for="novel in novels"
        :key="novel.id"
        :novel="novel"
        @read="startReading"
        @collect="toggleCollect"
      />
    </div>
    
    <!-- 空状态 -->
    <EmptyState
      v-else
      type="search-no-results"
      @action="goToRecommend"
    />
    
    <!-- 返回顶部 -->
    <BackToTop />
  </div>
</template>

<script setup>
import { ref } from 'vue'
import toast from '@/utils/toast'
import Breadcrumb from '@/components/common/Breadcrumb.vue'
import FilterPanel from '@/components/novel/FilterPanel.vue'
import SkeletonScreen from '@/components/common/SkeletonScreen.vue'
import NovelCardEnhanced from '@/components/novel/NovelCardEnhanced.vue'
import EmptyState from '@/components/common/EmptyState.vue'
import BackToTop from '@/components/common/BackToTop.vue'

const loading = ref(false)
const novels = ref([])

function toggleCollect(novel) {
  // 收藏逻辑
  toast.success('addToBookshelf')
}
</script>
```

---

### 阅读页

```vue
<template>
  <div class="reading-page">
    <!-- 阅读进度 -->
    <ReadingProgress
      :current-chapter="currentChapter"
      :total-chapters="totalChapters"
      :session-read-time="sessionTime"
      :today-read-time="todayTime"
    />
    
    <!-- 阅读内容 -->
    <div class="reading-content">
      <Loading
        v-if="loading"
        variant="book"
        text="正在加载章节..."
      />
      
      <div v-else class="chapter-content">
        {{ chapterContent }}
      </div>
    </div>
    
    <!-- 护眼设置 -->
    <EyeCareMode
      v-if="showSettings"
      :settings="eyeCareSettings"
      @update:settings="updateEyeCare"
    />
    
    <!-- 阅读预设 -->
    <ReadingPresets
      v-if="showPresets"
      @preset-change="handlePresetChange"
    />
    
    <!-- 键盘帮助 -->
    <KeyboardHelp
      :show="showKeyboardHelp"
      @update:show="showKeyboardHelp = $event"
    />
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useReadingKeyboard } from '@/composables/useKeyboard'
import ReadingProgress from '@/components/reading/ReadingProgress.vue'
import EyeCareMode from '@/components/reading/EyeCareMode.vue'
import ReadingPresets from '@/components/reading/ReadingPresets.vue'
import KeyboardHelp from '@/components/common/KeyboardHelp.vue'

const showKeyboardHelp = ref(false)

// 阅读页快捷键
useReadingKeyboard({
  onPrevChapter: prevChapter,
  onNextChapter: nextChapter,
  onToggleFullscreen: toggleFullscreen,
  onToggleSettings: () => showSettings.value = !showSettings.value
})
</script>
```

---

## 🎨 设计规范

### 颜色使用

```css
/* 主色调 */
--color-primary: #d95468       /* 玫瑰红 */
--color-primary-dark: #c23d52  /* 深玫瑰红 */
--color-primary-light: #ff6b7f /* 浅玫瑰红 */

/* 文字色 */
--color-text-primary: #2d2d2d     /* 主文字 */
--color-text-secondary: #5a5a5a   /* 次要文字 */
--color-text-muted: #8c8c8c       /* 弱化文字 */

/* 背景色 */
--color-bg-base: #fefefe          /* 基础背景 */
--color-bg-elevated: #ffffff      /* 抬升背景 */
--color-bg-hover: #f8f8f8         /* 悬停背景 */
```

### 间距规范

```css
/* 间距系统（8px 基准） */
0.25rem = 4px   /* 超小间距 */
0.5rem  = 8px   /* 小间距 */
0.75rem = 12px  /* 中小间距 */
1rem    = 16px  /* 标准间距 */
1.5rem  = 24px  /* 大间距 */
2rem    = 32px  /* 超大间距 */
```

### 圆角规范

```css
/* 圆角系统 */
border-radius: 4px    /* 小圆角 - 标签 */
border-radius: 8px    /* 中圆角 - 按钮/卡片 */
border-radius: 12px   /* 大圆角 - 面板 */
border-radius: 16px   /* 超大圆角 - 容器 */
border-radius: 24px   /* 全圆角 - 特殊按钮 */
border-radius: 50%    /* 圆形 - 头像/图标 */
```

### 字体规范

```css
/* 字体大小 */
0.75rem   = 12px   /* 辅助文字 */
0.8125rem = 13px   /* 次要文字 */
0.875rem  = 14px   /* 正文 */
0.9375rem = 15px   /* 强调正文 */
1rem      = 16px   /* 标准正文 */
1.125rem  = 18px   /* 小标题 */
1.5rem    = 24px   /* 标题 */
2rem      = 32px   /* 大标题 */
```

---

## 🔧 最佳实践

### 1. 性能优化

```vue
<!-- 使用图片懒加载 -->
<LazyImage :src="image" alt="描述" />

<!-- 使用骨架屏 -->
<SkeletonScreen v-if="loading" type="novel-card" />

<!-- 防抖搜索输入 -->
import { debounce } from 'lodash-es'
const handleSearch = debounce(searchFn, 300)
```

### 2. 无障碍访问

```vue
<!-- 语义化 HTML -->
<article role="article" aria-labelledby="title">
  <h3 id="title">标题</h3>
</article>

<!-- ARIA 标签 -->
<button
  aria-label="开始阅读小说"
  aria-pressed="false"
>
  阅读
</button>

<!-- 屏幕阅读器专用 -->
<span class="sr-only">仅屏幕阅读器可见的文本</span>
```

### 3. 响应式设计

```css
/* 移动优先 */
.component {
  /* 移动端样式 */
}

@media (min-width: 768px) {
  .component {
    /* 平板端样式 */
  }
}

@media (min-width: 1024px) {
  .component {
    /* 桌面端样式 */
  }
}
```

---

## 📱 移动端优化

### 触摸目标

**WCAG 2.1 AA 标准**: 最小触摸目标 44×44px

```css
button, a, .clickable {
  min-width: 44px;
  min-height: 44px;
}

/* 或通过 padding */
.btn {
  padding: 12px 16px;  /* 确保达到最小尺寸 */
}
```

### 安全区域

```css
/* iOS 刘海屏适配 */
.app-header {
  padding-top: calc(16px + env(safe-area-inset-top));
}

.bottom-nav {
  padding-bottom: calc(8px + env(safe-area-inset-bottom));
}
```

---

## ⚡ 快速上手

### 1. 引入组件

```javascript
// 在需要的页面引入
import Toast from '@/components/common/Toast.vue'
import Loading from '@/components/common/Loading.vue'
import EmptyState from '@/components/common/EmptyState.vue'
```

### 2. 注册全局组件（可选）

```javascript
// main.js
import { createApp } from 'vue'
import App from './App.vue'

// 引入组件
import Loading from '@/components/common/Loading.vue'
import EmptyState from '@/components/common/EmptyState.vue'

const app = createApp(App)

// 注册全局组件
app.component('Loading', Loading)
app.component('EmptyState', EmptyState)

app.mount('#app')
```

### 3. 使用工具函数

```javascript
// 在任何组件中
import toast from '@/utils/toast'
import { useKeyboard } from '@/composables/useKeyboard'

export default {
  setup() {
    // 显示提示
    toast.success('操作成功')
    
    // 键盘快捷键
    const { showKeyboardHelp } = useKeyboard()
    
    return {
      showKeyboardHelp
    }
  }
}
```

---

## 🎯 组件组合示例

### 完整的小说详情页

```vue
<template>
  <div class="novel-detail-page">
    <!-- 面包屑 -->
    <Breadcrumb />
    
    <!-- 加载中 -->
    <SkeletonScreen v-if="loading" type="novel-detail" />
    
    <!-- 内容 -->
    <div v-else-if="novel" class="novel-detail">
      <!-- 封面 -->
      <LazyImage
        :src="novel.cover"
        :alt="novel.title"
        class="cover"
      />
      
      <!-- 信息 -->
      <div class="info">
        <h1>{{ novel.title }}</h1>
        <p>{{ novel.author }}</p>
        
        <!-- 操作按钮 -->
        <div class="actions">
          <button @click="startReading">开始阅读</button>
          <LikeButton
            :liked="novel.isLiked"
            :count="novel.likes"
            @like="handleLike"
          />
        </div>
      </div>
    </div>
    
    <!-- 空状态 -->
    <EmptyState v-else type="notFound" @action="goHome" />
    
    <!-- 返回顶部 -->
    <BackToTop />
  </div>
</template>
```

---

## 📖 参考资料

- **设计规范**: Material Design 3 / Apple HIG
- **无障碍标准**: WCAG 2.1 AA
- **Vue 3 文档**: https://vuejs.org
- **Tailwind CSS**: https://tailwindcss.com

---

**文档编制**: 高级全栈工程师  
**更新日期**: 2025年10月28日  

---

*本指南将持续更新，欢迎提出改进建议。*

