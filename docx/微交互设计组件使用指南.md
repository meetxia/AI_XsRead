# 微交互设计组件使用指南

> **更新日期**: 2025年10月28日  
> **版本**: v1.0.0  
> **作者**: 全栈开发团队

---

## 📋 目录

1. [涟漪效果组件 (RippleEffect)](#涟漪效果组件)
2. [手势交互 (useGestures)](#手势交互)
3. [下拉刷新组件 (PullToRefresh)](#下拉刷新组件)
4. [进度条组件 (ProgressBar)](#进度条组件)
5. [触觉反馈指令 (v-feedback)](#触觉反馈指令)

---

## 🌊 涟漪效果组件

### 组件路径
```
src/components/common/RippleEffect.vue
```

### 基础用法

```vue
<template>
  <ripple-effect>
    <button class="my-button">点击我</button>
  </ripple-effect>
</template>

<script setup>
import RippleEffect from '@/components/common/RippleEffect.vue'
</script>
```

### Props

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| color | String | `rgba(217, 84, 104, 0.3)` | 涟漪颜色 |
| duration | Number | `600` | 动画持续时间(ms) |
| disabled | Boolean | `false` | 是否禁用 |
| center | Boolean | `false` | 是否从中心扩散 |

### 示例

```vue
<!-- 自定义颜色 -->
<ripple-effect color="rgba(16, 185, 129, 0.3)">
  <button>绿色涟漪</button>
</ripple-effect>

<!-- 中心扩散 -->
<ripple-effect center>
  <button>中心扩散</button>
</ripple-effect>

<!-- 更长的动画时间 -->
<ripple-effect :duration="1000">
  <button>慢速涟漪</button>
</ripple-effect>
```

### 暴露的方法

```vue
<template>
  <ripple-effect ref="rippleRef">
    <button @click="triggerRipple">手动触发</button>
  </ripple-effect>
</template>

<script setup>
import { ref } from 'vue'

const rippleRef = ref(null)

function triggerRipple(event) {
  rippleRef.value?.createRipple(event)
}
</script>
```

---

## 👆 手势交互

### Composable路径
```
src/composables/useGestures.js
```

### 基础用法

```vue
<template>
  <div ref="gestureBox">
    滑动、双击或长按此区域
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useGestures } from '@/composables/useGestures'

const gestureBox = ref(null)

onMounted(() => {
  useGestures(gestureBox, {
    swipeLeft: () => console.log('向左滑动'),
    swipeRight: () => console.log('向右滑动'),
    swipeUp: () => console.log('向上滑动'),
    swipeDown: () => console.log('向下滑动'),
    doubleTap: () => console.log('双击'),
    longPress: () => console.log('长按'),
    pinch: ({ scale }) => console.log('捏合缩放:', scale)
  })
})
</script>
```

### 简化版 API

#### useSwipe - 滑动手势

```javascript
import { useSwipe } from '@/composables/useGestures'

useSwipe(targetRef, {
  onSwipeLeft: () => nextChapter(),
  onSwipeRight: () => prevChapter(),
  onSwipeUp: () => scrollUp(),
  onSwipeDown: () => scrollDown()
})
```

#### useDoubleTap - 双击手势

```javascript
import { useDoubleTap } from '@/composables/useGestures'

useDoubleTap(targetRef, () => {
  toggleFullscreen()
})
```

#### useLongPress - 长按手势

```javascript
import { useLongPress } from '@/composables/useGestures'

useLongPress(targetRef, () => {
  showContextMenu()
}, 800) // 800ms触发
```

#### usePinch - 捏合缩放手势

```javascript
import { usePinch } from '@/composables/useGestures'

usePinch(targetRef, ({ scale }) => {
  adjustFontSize(scale)
})
```

### 配置选项

```javascript
useGestures(targetRef, handlers, {
  swipeThreshold: 50,      // 最小滑动距离(px)
  swipeTimeout: 300,       // 最大滑动时间(ms)
  doubleTapDelay: 300,     // 双击间隔时间(ms)
  longPressDelay: 500,     // 长按时间(ms)
  pinchThreshold: 0.1,     // 捏合缩放阈值
  preventDefault: true     // 是否阻止默认行为
})
```

### 阅读页应用示例

```vue
<template>
  <div ref="readingArea" class="reading-area">
    <!-- 阅读内容 -->
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useGestures } from '@/composables/useGestures'
import { useRouter } from 'vue-router'

const readingArea = ref(null)
const router = useRouter()
const showControls = ref(true)

onMounted(() => {
  useGestures(readingArea, {
    // 左右滑动切换章节
    swipeLeft: () => nextChapter(),
    swipeRight: () => prevChapter(),
    
    // 双击显示/隐藏控制栏
    doubleTap: () => {
      showControls.value = !showControls.value
    },
    
    // 长按显示菜单
    longPress: () => {
      showContextMenu()
    },
    
    // 捏合缩放字体
    pinch: ({ scale }) => {
      adjustFontSize(scale)
    }
  })
})

function nextChapter() {
  // 切换到下一章
}

function prevChapter() {
  // 切换到上一章
}
</script>
```

---

## 🔄 下拉刷新组件

### 组件路径
```
src/components/common/PullToRefresh.vue
```

### 基础用法

```vue
<template>
  <pull-to-refresh @refresh="handleRefresh">
    <div class="content-list">
      <div v-for="item in items" :key="item.id">
        {{ item.text }}
      </div>
    </div>
  </pull-to-refresh>
</template>

<script setup>
import { ref } from 'vue'
import PullToRefresh from '@/components/common/PullToRefresh.vue'
import toast from '@/utils/toast'

const items = ref([])

async function handleRefresh() {
  // 模拟数据加载
  await new Promise(resolve => setTimeout(resolve, 1500))
  
  // 加载新数据
  const newData = await fetchNewData()
  items.value = [...newData, ...items.value]
  
  toast.success('刷新成功 ✨')
}
</script>
```

### Props

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| threshold | Number | `80` | 触发刷新的阈值(px) |
| maxDistance | Number | `150` | 最大下拉距离(px) |
| disabled | Boolean | `false` | 是否禁用 |
| resistance | Number | `0.5` | 阻力系数(0-1) |
| autoClose | Boolean | `true` | 是否自动关闭 |
| successDuration | Number | `800` | 成功提示显示时长(ms) |

### Events

| 事件名 | 说明 | 回调参数 |
|--------|------|---------|
| refresh | 触发刷新时触发 | - |

### 暴露的方法

```vue
<template>
  <pull-to-refresh ref="pullRefreshRef" @refresh="handleRefresh">
    <!-- 内容 -->
  </pull-to-refresh>
  
  <button @click="resetRefresh">手动重置</button>
</template>

<script setup>
import { ref } from 'vue'

const pullRefreshRef = ref(null)

function resetRefresh() {
  pullRefreshRef.value?.reset()
}
</script>
```

### 完整示例

```vue
<template>
  <div class="page">
    <pull-to-refresh 
      :threshold="100"
      :max-distance="200"
      :resistance="0.6"
      @refresh="loadNewContent"
    >
      <div class="novel-list">
        <novel-card 
          v-for="novel in novels" 
          :key="novel.id"
          :novel="novel"
        />
      </div>
    </pull-to-refresh>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import PullToRefresh from '@/components/common/PullToRefresh.vue'
import NovelCard from '@/components/novel/NovelCard.vue'
import { fetchLatestNovels } from '@/api/novel'

const novels = ref([])

async function loadNewContent() {
  try {
    const newNovels = await fetchLatestNovels()
    novels.value = [...newNovels, ...novels.value]
  } catch (error) {
    console.error('加载失败:', error)
  }
}
</script>
```

---

## 📊 进度条组件

### 组件路径
```
src/components/common/ProgressBar.vue
```

### 基础用法

```vue
<template>
  <progress-bar :percent="uploadProgress" />
</template>

<script setup>
import { ref } from 'vue'
import ProgressBar from '@/components/common/ProgressBar.vue'

const uploadProgress = ref(0)

// 模拟上传进度
setInterval(() => {
  if (uploadProgress.value < 100) {
    uploadProgress.value += 10
  }
}, 500)
</script>
```

### Props

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| percent | Number | `0` | 进度百分比(0-100) |
| showText | Boolean | `true` | 是否显示进度文本 |
| showStatus | Boolean | `false` | 是否显示状态 |
| status | String | `''` | 状态(success/error/warning) |
| statusText | String | `''` | 状态文本 |
| indeterminate | Boolean | `false` | 不确定进度 |
| loadingText | String | `'加载中...'` | 加载文本 |
| animated | Boolean | `true` | 是否显示动画 |
| color | String | `''` | 自定义颜色 |
| height | String | `'8px'` | 进度条高度 |
| size | String | `'medium'` | 尺寸(small/medium/large) |

### 示例

#### 普通进度条

```vue
<progress-bar :percent="50" />
```

#### 不确定进度

```vue
<progress-bar indeterminate loading-text="正在处理..." />
```

#### 带状态的进度条

```vue
<progress-bar 
  :percent="100" 
  show-status 
  status="success" 
  status-text="上传完成" 
/>

<progress-bar 
  :percent="45" 
  show-status 
  status="error" 
  status-text="上传失败" 
/>
```

#### 不同尺寸

```vue
<progress-bar :percent="60" size="small" />
<progress-bar :percent="60" size="medium" />
<progress-bar :percent="60" size="large" />
```

#### 自定义颜色

```vue
<progress-bar :percent="75" color="#10b981" />
<progress-bar :percent="75" color="linear-gradient(90deg, #667eea 0%, #764ba2 100%)" />
```

### 实际应用示例

#### 文件上传进度

```vue
<template>
  <div class="upload-container">
    <input type="file" @change="handleFileUpload" />
    
    <progress-bar 
      v-if="uploading"
      :percent="uploadProgress"
      :show-status="uploadStatus !== 'uploading'"
      :status="uploadStatus"
      :status-text="statusText"
    />
  </div>
</template>

<script setup>
import { ref } from 'vue'
import ProgressBar from '@/components/common/ProgressBar.vue'
import { uploadFile } from '@/api/upload'

const uploading = ref(false)
const uploadProgress = ref(0)
const uploadStatus = ref('uploading')
const statusText = ref('')

async function handleFileUpload(event) {
  const file = event.target.files[0]
  if (!file) return
  
  uploading.value = true
  uploadProgress.value = 0
  uploadStatus.value = 'uploading'
  
  try {
    await uploadFile(file, (progress) => {
      uploadProgress.value = progress
    })
    
    uploadStatus.value = 'success'
    statusText.value = '上传成功'
  } catch (error) {
    uploadStatus.value = 'error'
    statusText.value = '上传失败'
  }
}
</script>
```

---

## 📳 触觉反馈指令

### 指令路径
```
src/directives/feedback.js
```

### 全局注册

指令已在 `main.js` 中全局注册，可直接使用：

```javascript
// main.js
import { registerFeedbackDirective } from './directives/feedback'

registerFeedbackDirective(app)
```

### 基础用法

```vue
<template>
  <!-- 默认配置：涟漪+触觉+缩放 -->
  <button v-feedback>点击我</button>
</template>
```

### 配置选项

```vue
<template>
  <!-- 自定义配置 -->
  <button v-feedback="{
    haptic: true,      // 触觉反馈
    ripple: true,      // 涟漪效果
    scale: true,       // 缩放效果
    sound: null,       // 音效文件
    duration: 150,     // 动画时长
    rippleColor: 'rgba(217, 84, 104, 0.3)'  // 涟漪颜色
  }">
    完整反馈
  </button>

  <!-- 仅触觉反馈 -->
  <button v-feedback="{ haptic: true, ripple: false, scale: false }">
    仅震动
  </button>

  <!-- 仅涟漪效果 -->
  <button v-feedback="{ ripple: true, haptic: false, scale: false }">
    仅涟漪
  </button>

  <!-- 自定义颜色 -->
  <button v-feedback="{ 
    rippleColor: 'rgba(16, 185, 129, 0.3)' 
  }">
    绿色涟漪
  </button>
</template>
```

### 实际应用

#### 小说卡片

```vue
<template>
  <div class="novel-card">
    <img :src="novel.cover" :alt="novel.title" />
    
    <h3>{{ novel.title }}</h3>
    
    <div class="actions">
      <button 
        v-feedback="{ rippleColor: 'rgba(217, 84, 104, 0.3)' }"
        @click="startReading"
      >
        开始阅读
      </button>
      
      <button 
        v-feedback="{ rippleColor: 'rgba(16, 185, 129, 0.3)' }"
        @click="addToBookshelf"
      >
        加入书架
      </button>
    </div>
  </div>
</template>
```

#### 底部导航

```vue
<template>
  <nav class="bottom-nav">
    <router-link 
      to="/"
      v-feedback="{ scale: false, ripple: true }"
    >
      <i class="icon-home"></i>
      <span>首页</span>
    </router-link>
    
    <router-link 
      to="/bookshelf"
      v-feedback="{ scale: false, ripple: true }"
    >
      <i class="icon-book"></i>
      <span>书架</span>
    </router-link>
    
    <router-link 
      to="/profile"
      v-feedback="{ scale: false, ripple: true }"
    >
      <i class="icon-user"></i>
      <span>我的</span>
    </router-link>
  </nav>
</template>
```

---

## 🎯 最佳实践

### 1. 性能优化

```vue
<!-- ❌ 不好的做法：在列表项中使用复杂动画 -->
<div v-for="item in largeList" :key="item.id">
  <ripple-effect :duration="2000">
    <button>{{ item.name }}</button>
  </ripple-effect>
</div>

<!-- ✅ 好的做法：使用轻量级的v-feedback指令 -->
<div v-for="item in largeList" :key="item.id">
  <button v-feedback>{{ item.name }}</button>
</div>
```

### 2. 移动端优化

```vue
<template>
  <div class="mobile-page">
    <!-- 使用下拉刷新 -->
    <pull-to-refresh @refresh="loadNewData">
      <div class="content">
        <!-- 内容 -->
      </div>
    </pull-to-refresh>
    
    <!-- 使用手势导航 -->
    <div ref="readingArea">
      <!-- 阅读内容 -->
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useSwipe } from '@/composables/useGestures'

const readingArea = ref(null)

onMounted(() => {
  // 移动端友好的手势
  useSwipe(readingArea, {
    onSwipeLeft: () => nextPage(),
    onSwipeRight: () => prevPage()
  })
})
</script>
```

### 3. 无障碍访问

```vue
<template>
  <!-- 确保涟漪效果不影响无障碍 -->
  <ripple-effect>
    <button 
      aria-label="开始阅读小说"
      @click="startReading"
    >
      开始阅读
    </button>
  </ripple-effect>
  
  <!-- 进度条添加ARIA属性 -->
  <progress-bar 
    :percent="loadProgress"
    role="progressbar"
    :aria-valuenow="loadProgress"
    aria-valuemin="0"
    aria-valuemax="100"
  />
</template>
```

---

## 📱 移动端注意事项

### 1. 触觉反馈

```javascript
// 触觉反馈仅在支持的设备上工作
if ('vibrate' in navigator) {
  navigator.vibrate(10) // 震动10ms
}
```

### 2. 手势冲突

```vue
<script setup>
// 防止手势与原生滚动冲突
useGestures(targetRef, handlers, {
  preventDefault: false  // 在需要保留原生滚动时设为false
})
</script>
```

### 3. 性能考虑

```vue
<!-- 在低端设备上禁用复杂动画 -->
<ripple-effect :disabled="isLowEndDevice">
  <button>点击我</button>
</ripple-effect>

<script setup>
import { ref, onMounted } from 'vue'

const isLowEndDevice = ref(false)

onMounted(() => {
  // 检测设备性能
  const memory = navigator.deviceMemory || 4
  isLowEndDevice.value = memory < 4
})
</script>
```

---

## 🎨 样式定制

### CSS变量

所有组件都支持通过CSS变量进行主题定制：

```css
:root {
  /* 主色 */
  --color-primary: #d95468;
  
  /* 背景色 */
  --color-bg-base: #fefefe;
  --color-bg-elevated: #ffffff;
  --color-bg-hover: #f8f8f8;
  
  /* 文字色 */
  --color-text-primary: #2d2d2d;
  --color-text-secondary: #5a5a5a;
  --color-text-muted: #8c8c8c;
  
  /* 边框色 */
  --color-border: #e5e7eb;
}

/* 暗色模式 */
:root.dark {
  --color-bg-base: #1a1a1a;
  --color-bg-elevated: #2d2d2d;
  --color-text-primary: #e8e8e8;
  /* ... */
}
```

---

## 🔧 故障排除

### 1. 涟漪效果不显示

**问题**: 点击按钮没有涟漪效果

**解决方案**:
```vue
<!-- 确保容器有position属性 -->
<ripple-effect>
  <button style="position: relative;">点击我</button>
</ripple-effect>
```

### 2. 手势不触发

**问题**: 手势事件没有响应

**解决方案**:
```vue
<script setup>
// 确保元素ref已正确绑定
const gestureBox = ref(null)

onMounted(() => {
  // 检查元素是否存在
  if (!gestureBox.value) {
    console.error('手势元素未找到')
    return
  }
  
  useGestures(gestureBox, handlers)
})
</script>
```

### 3. 下拉刷新不工作

**问题**: 下拉刷新无法触发

**解决方案**:
```vue
<!-- 确保容器可滚动 -->
<pull-to-refresh style="overflow-y: auto; height: 100vh;">
  <div class="content">
    <!-- 内容 -->
  </div>
</pull-to-refresh>
```

---

## 📚 相关文档

- [用户体验增强报告](./本项目研究报告-项目经理/3-用户体验增强报告.md)
- [Vue3 组件开发指南](https://cn.vuejs.org/guide/components.html)
- [移动端触摸事件](https://developer.mozilla.org/zh-CN/docs/Web/API/Touch_events)

---

## 💡 贡献指南

如果你有更好的微交互设计想法，欢迎：

1. 提交 Issue 讨论
2. 创建 Pull Request
3. 完善文档和示例

---

**最后更新**: 2025年10月28日  
**维护团队**: AI_XsRead 开发团队

